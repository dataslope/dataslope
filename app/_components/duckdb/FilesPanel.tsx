/** Re-exports the shared FilesPanel (canonical implementation in
 *  app/_components/files/FilesPanel.tsx) so existing import paths work. */
export { FilesPanel, type VirtualFile } from "../files/FilesPanel";
