---
title: Loading Animations
description: Brand loading animations built from the Dataslope logo, diamond spinners in several styles and color variants.
---

## Overview

Every animation on this page is built from the Dataslope logo mark (`public/dataslope-logo-blue.svg`) and one fact about its geometry:

- **Diamond.** Stacking the mark on a vertically mirrored copy makes both copies' rounded foot caps coincide, forming a 4-point star whose centre opens into a small **hollow diamond**. The shapes keep the logo's translucent radial gradients, so the four overlapping swooshes show through as lighter petals around that hollow.

All variants draw with `currentColor` (brand blue by default, light/dark aware) and honour `prefers-reduced-motion` by replacing motion with a gentle opacity pulse.

## Spinners

<LoadingAnimationsGallery section="spinners" />

## Color variants

Every loader accepts a `color` prop, so the mark can be tinted to any CSS color, use the Dataslope palette (`var(--ds-green-500)`, `var(--ds-red-500)`, …) or plain black/white for overlays. Shown on the "Diamond assemble + quarter-turn" loader:

<LoadingAnimationsGallery section="variants" />

## Using the components

The components live in `app/_components/mdx/loadingAnimations.tsx` and can be imported anywhere in the app:

```tsx
import {
  DiamondSpinner,
  DiamondTurnSpinner,
  DiamondAssembleLoader,
  DiamondAssembleTurnLoader,
  DiamondRippleLoader,
  LogoHopLoader,
} from "@/app/_components/mdx/loadingAnimations";

// Small inline spinner (e.g. beside a status message)
<DiamondSpinner size={20} />

// Assemble + quarter-turn sequence for longer "preparing…" waits
<DiamondAssembleTurnLoader size={72} />

// Tinted with a brand-palette color
<DiamondAssembleTurnLoader size={72} color="var(--ds-green-500)" />
```

Every component accepts a `label` prop for its accessible name (default `"Loading…"`) and a `color` prop to override the brand-blue tint; the continuous spinner also accepts `duration` (seconds per revolution).
