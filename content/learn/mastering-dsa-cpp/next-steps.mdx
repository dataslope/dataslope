---
title: Next Steps
description: Where to go after mastering data structures and algorithms in C++
---

Congratulations. You have traveled from foundations to linear structures, recursion, search, sort, hashing, trees, graphs, and algorithmic paradigms — and picked up some history on the way: Hoare's Moscow sorting problem, Dijkstra's café, Huffman's term paper, Bloch's overflow bug. If one meta-lesson is worth keeping, it is the one those stories share: every structure in this course is a *trade*, invented by someone who needed one operation to be cheap and accepted that another would get expensive. When you face a new problem, ask which operations are hot, and the right structure usually names itself.

<svg
  viewBox="0 0 640 230"
  role="img"
  aria-label="A rocket lifts off its pad toward three distant destination dots along faint dotted trails — the course complete and several directions to fly next"
  style={{ display: "block", width: "100%", maxWidth: "560px", height: "auto", margin: "2.5rem auto" }}
>
  <g fill="currentColor" opacity="0.2">
    <circle cx="90" cy="88" r="2" />
    <circle cx="556" cy="84" r="2" />
    <circle cx="160" cy="140" r="2" />
    <circle cx="490" cy="150" r="2" />
  </g>
  <g fill="currentColor" opacity="0.3">
    <circle cx="260" cy="51" r="2.5" />
    <circle cx="200" cy="48" r="2.5" />
    <circle cx="320" cy="47" r="2.5" />
    <circle cx="380" cy="51" r="2.5" />
    <circle cx="440" cy="48" r="2.5" />
  </g>
  <g style={{ color: "var(--ds-green-500)" }} fill="currentColor">
    <circle cx="140" cy="42" r="8" />
  </g>
  <g style={{ color: "var(--ds-yellow-500)" }} fill="currentColor">
    <circle cx="320" cy="28" r="8" />
  </g>
  <g style={{ color: "var(--ds-blue-500)" }} fill="currentColor">
    <circle cx="500" cy="42" r="8" />
    <polygon points="302,100 338,100 320,60" />
    <rect x="302" y="92" width="36" height="70" rx="14" />
    <polygon points="302,142 302,168 282,172" fillOpacity="0.7" />
    <polygon points="338,142 338,168 358,172" fillOpacity="0.7" />
  </g>
  <g style={{ fill: "var(--ds-blue-50)" }}>
    <circle cx="320" cy="118" r="7" />
  </g>
  <g style={{ color: "var(--ds-yellow-500)" }} fill="currentColor">
    <polygon points="308,162 332,162 320,188" />
    <circle cx="320" cy="196" r="3" fillOpacity="0.5" />
    <circle cx="320" cy="204" r="2" fillOpacity="0.3" />
  </g>
  <rect x="240" y="208" width="160" height="6" rx="3" fill="currentColor" opacity="0.15" />
</svg>

<Callout type="success" title="Well done">
You now have the toolkit to read serious algorithmic code, solve interview-style problems, and build systems that depend on efficient data structures. Thank you for learning with this course.
</Callout>

What follows is a map, not a syllabus — pick the direction that matches what you want to build, and go deep rather than wide.

## Advanced data structures

Explore segment trees and Fenwick trees (range queries with updates — the natural next step after heaps), suffix arrays (the string-search workhorse), sparse tables, treaps, and splay trees.

## Advanced graph algorithms

Study Bellman-Ford, Floyd-Warshall, A*, max-flow with Edmonds-Karp and Dinic, Tarjan/Kosaraju SCC, articulation points, bridges, and 2-SAT.

## String algorithms

Learn KMP, Z-algorithm, Aho-Corasick, suffix automata, and Manacher's algorithm.

## Computational geometry

Try convex hulls with Graham scan and Andrew's monotone chain, sweep line methods, and line intersection.

## Randomized and approximation algorithms

Look into randomized quicksort, randomized hashing, Bloom filters, and MCMC.

## Parallel and concurrent DSA

Study lock-free queues, persistent data structures, concurrent hash maps, and the memory-model ideas behind them.

## Modern C++ worth investing in

- Move semantics and rvalue references
- Smart pointers: `unique_ptr` and `shared_ptr`
- C++20 ranges and views
- Templates and concepts
- Coroutines in C++20

## Where to practice

LeetCode, Codeforces, AtCoder, HackerRank, and USACO all give different styles of repetition. Also build toy projects: a JSON parser, a key-value store, or a simple compiler. Each forces you to use DSA in a real design.

## Books worth reading

- CLRS
- Algorithms by Sedgewick
- Competitive Programming by Halim
- Algorithm Design by Kleinberg-Tardos
- The Algorithm Design Manual by Skiena
- Effective Modern C++ by Scott Meyers
- C++ Primer

## A final C++ wave-off

One last runnable program, as a send-off. It is nothing exotic — a frequency count, a custom-sorted vector of pairs, a tidy printout — but notice how many chapters it quietly uses: hashing-by-tree (`std::map`), introsort (`std::sort`), a lambda comparator with a tiebreak, amortized vector growth. Fluency is when this reads as one thought.

<CodeBlock
  adapter="cpp"
  files={[
    {
      filename: "main.cpp",
      starterCode: `#include <algorithm>
#include <iostream>
#include <map>
#include <string>
#include <vector>

int main() {
  std::vector<std::string> words = {"tree", "graph", "tree",
                                    "dp",   "graph", "tree"};
  std::map<std::string, int> freq;
  for (const auto &word : words)
    freq[word]++;

  std::vector<std::pair<std::string, int>> items(freq.begin(), freq.end());
  std::sort(items.begin(), items.end(), [](const auto &a, const auto &b) {
    if (a.second != b.second)
      return a.second > b.second;
    return a.first < b.first;
  });

  for (const auto &[word, count] : items) {
    std::cout << word << ": " << count << "\\n";
  }
  return 0;
}
`,
    },
  ]}
/>

<MultipleChoice markdown={`What is a good next step after this course?

- Stop practicing algorithms forever.
  > Skills fade without use.
- [o] Pick a roadmap topic and build or solve problems with it.
  > Practice makes the ideas durable.
- Only memorize code templates.
  > Understanding matters more.
- Avoid modern C++ features.
  > Modern C++ helps express solutions safely.`} />
