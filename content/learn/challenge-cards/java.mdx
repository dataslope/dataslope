---
title: Challenge Cards — Java
description: Sandbox for `<ChallengeCard>` Java variations.
---

Java challenges compile in the browser via **CheerpJ** (an
in-browser OpenJDK). Tests use the stdout-based mode because the JVM
process exits after `main` returns.

The class name in your source must match the filename CheerpJ derives
from it. Keep the class named `Main` for these exercises.

## 1. Hello, Java

<ChallengeCard
  adapter="java"
  title="Print a greeting"
  category="basics · System.out"
  estimatedTime="~1 min"
  instructions={`Make the program print exactly \`Hello, Java!\`.`}
  initialCode={`public class Main {
    public static void main(String[] args) {
        // your code here
    }
}
`}
  solutionCode={`public class Main {
    public static void main(String[] args) {
        System.out.println("Hello, Java!");
    }
}
`}
  tests={[
    {
      id: "hello",
      name: "Prints `Hello, Java!`",
      description: "Exact stdout match",
      expect: { stdoutEquals: "Hello, Java!" },
    },
  ]}
/>

```jsx
<ChallengeCard
  adapter="java"
  title="Print a greeting"
  category="basics · System.out"
  estimatedTime="~1 min"
  instructions={`Make the program print exactly \`Hello, Java!\`.`}
  initialCode={`public class Main {
    public static void main(String[] args) {
        // your code here
    }
}
`}
  solutionCode={`public class Main {
    public static void main(String[] args) {
        System.out.println("Hello, Java!");
    }
}
`}
  tests={[
    {
      id: "hello",
      name: "Prints `Hello, Java!`",
      description: "Exact stdout match",
      expect: { stdoutEquals: "Hello, Java!" },
    },
  ]}
/>
```

## 2. String manipulation

<ChallengeCard
  adapter="java"
  title="Reverse a String"
  category="strings"
  estimatedTime="~3 min"
  instructions={`Reverse the string \`"DataSlope"\` and print it on its own line.`}
  initialCode={`public class Main {
    public static void main(String[] args) {
        String s = "DataSlope";
        // your code here
    }
}
`}
  solutionCode={`public class Main {
    public static String reverse(String s) {
        return new StringBuilder(s).reverse().toString();
    }
    public static void main(String[] args) {
        System.out.println(reverse("hello"));
        System.out.println(reverse("racecar"));
    }
}
`}
  tests={[
    {
      id: "reversed",
      name: "Prints reversed string",
      description: "Output: `epolSataD`",
      expect: { stdoutEquals: "epolSataD" },
    },
  ]}
/>

```jsx
<ChallengeCard
  adapter="java"
  title="Reverse a String"
  category="strings"
  estimatedTime="~3 min"
  instructions={`Reverse the string \`"DataSlope"\` and print it on its own line.`}
  initialCode={`public class Main {
    public static void main(String[] args) {
        String s = "DataSlope";
        // your code here
    }
}
`}
  solutionCode={`public class Main {
    public static String reverse(String s) {
        return new StringBuilder(s).reverse().toString();
    }
    public static void main(String[] args) {
        System.out.println(reverse("hello"));
        System.out.println(reverse("racecar"));
    }
}
`}
  tests={[
    {
      id: "reversed",
      name: "Prints reversed string",
      description: "Output: `epolSataD`",
      expect: { stdoutEquals: "epolSataD" },
    },
  ]}
/>
```

## 3. Loops and conditionals

<ChallengeCard
  adapter="java"
  title="Print even numbers"
  category="loops · conditionals"
  estimatedTime="~2 min"
  instructions={`Print every even number from **2** to **10** (inclusive), one per line.`}
  initialCode={`public class Main {
    public static void main(String[] args) {
        // your code here
    }
}
`}
  solutionCode={`public class Main {
    public static void main(String[] args) {
        for (int i = 2; i <= 10; i += 2) {
            System.out.println(i);
        }
    }
}
`}
  tests={[
    {
      id: "evens",
      name: "Prints 2, 4, 6, 8, 10",
      description: "Five lines, one per even number",
      expect: {
        stdoutLines: ["2", "4", "6", "8", "10"],
        stdoutLinesExact: true,
      },
    },
  ]}
/>

```jsx
<ChallengeCard
  adapter="java"
  title="Print even numbers"
  category="loops · conditionals"
  estimatedTime="~2 min"
  instructions={`Print every even number from **2** to **10** (inclusive), one per line.`}
  initialCode={`public class Main {
    public static void main(String[] args) {
        // your code here
    }
}
`}
  solutionCode={`public class Main {
    public static void main(String[] args) {
        for (int i = 2; i <= 10; i += 2) {
            System.out.println(i);
        }
    }
}
`}
  tests={[
    {
      id: "evens",
      name: "Prints 2, 4, 6, 8, 10",
      description: "Five lines, one per even number",
      expect: {
        stdoutLines: ["2", "4", "6", "8", "10"],
        stdoutLinesExact: true,
      },
    },
  ]}
/>
```
