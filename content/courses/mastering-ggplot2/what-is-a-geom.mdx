---
title: What Is a Geom?
description: Geometries are the marks ggplot2 draws — understand them as interchangeable representations of the same data and mappings.
---

A **geom** (geometry) is the kind of **mark** ggplot2 draws to
represent your data: a point, a line, a bar, a box. It is the most
visible component — when someone says "make a scatter plot" or "make a
bar chart," they are really choosing a *geom*.

<svg
  viewBox="0 0 640 240"
  role="img"
  aria-label="One data card of four raw value bars connects by thin gray lines to three geom panels that draw the same values as blue points, green bars, and a yellow line"
  style={{ display: "block", width: "100%", maxWidth: "640px", height: "auto", margin: "2.5rem auto" }}
>
  <g stroke="var(--ds-gray-300)" strokeWidth="2">
    <line x1="300" y1="68" x2="340" y2="68" />
    <line x1="185" y1="112" x2="185" y2="128" />
    <line x1="300" y1="112" x2="340" y2="128" />
  </g>
  <rect x="70" y="24" width="230" height="88" rx="10" fill="currentColor" opacity="0.05" />
  <g fill="currentColor" opacity="0.18">
    <rect x="94" y="40" width="49" height="9" rx="4.5" />
    <rect x="94" y="58" width="112" height="9" rx="4.5" />
    <rect x="94" y="76" width="77" height="9" rx="4.5" />
    <rect x="94" y="94" width="140" height="9" rx="4.5" />
  </g>
  <g style={{ color: "var(--ds-blue-500)" }} fill="currentColor">
    <rect x="340" y="24" width="230" height="88" rx="10" fillOpacity="0.06" />
    <circle cx="380" cy="81" r="6" />
    <circle cx="430" cy="56" r="6" />
    <circle cx="480" cy="70" r="6" />
    <circle cx="530" cy="45" r="6" />
  </g>
  <g style={{ color: "var(--ds-green-500)" }} fill="currentColor">
    <rect x="70" y="128" width="230" height="88" rx="10" fillOpacity="0.06" />
    <rect x="100" y="183" width="18" height="21" rx="3" />
    <rect x="150" y="156" width="18" height="48" rx="3" />
    <rect x="200" y="171" width="18" height="33" rx="3" />
    <rect x="250" y="144" width="18" height="60" rx="3" />
  </g>
  <g style={{ color: "var(--ds-yellow-500)" }} fill="currentColor">
    <rect x="340" y="128" width="230" height="88" rx="10" fillOpacity="0.1" />
    <polyline points="380,181 430,156 480,170 530,145" fill="none" stroke="currentColor" strokeWidth="3.5" strokeLinecap="round" strokeLinejoin="round" />
  </g>
</svg>

## One data + mapping, many geoms

Here is the liberating idea. The data and mappings describe *what
variables relate to what visual channels*. The geom decides *how those
relationships are drawn*. Swap the geom and you get a different chart
**from the same description**.

From the *same* data and mappings — `aes(x = displ, y = hwy)` — each
geom draws a different chart:

- `geom_point` — a scatter plot.
- `geom_line` — a connected line.
- `geom_bin2d` — a 2-D density.
- `geom_smooth` — a trend line + band.

Watch the geom change while everything else stays fixed:

<CodeBlock
  adapter="r"
  files={[
    {
      filename: "main.r",
      starterCode: `library(ggplot2)

base <- ggplot(mpg, aes(displ, hwy))

base + geom_point() # marks = points  -> scatter plot
`,
    },
  ]}
/>

<CodeBlock
  adapter="r"
  files={[
    {
      filename: "main.r",
      starterCode: `library(ggplot2)

base <- ggplot(mpg, aes(displ, hwy))

base + geom_smooth() # marks = a smoothed trend line + confidence band
`,
    },
  ]}
/>

You changed exactly one word and the chart's *character* changed
completely — yet the data and mappings are identical. That is the geom
doing its one job: choosing the mark.

## Geoms have required aesthetics

Each geom needs certain aesthetics to make sense, and ignores others:

| Geom | Needs | Draws |
| --- | --- | --- |
| `geom_point()` | x, y | a dot per row |
| `geom_line()` | x, y | a line through points in x order |
| `geom_col()` | x, y | a bar of height y at each x |
| `geom_bar()` | x | a bar whose height is the **count** at each x |
| `geom_histogram()` | x | bars over **binned** counts of x |
| `geom_boxplot()` | x (group), y | a five-number-summary box per group |
| `geom_tile()` | x, y, fill | a colored cell — heatmaps |

Notice `geom_bar()` and `geom_histogram()` need only **x**. That is a
clue we will unpack in the next section: those geoms *compute* their y
themselves, via a statistic.

## A line needs an order; a scatter does not

A geom is not just a different look — it carries *assumptions*. Points
are unordered: each is independent. A line **connects points in x
order**, which only makes sense when x has a meaningful sequence (like
time). Using a line on unordered data produces the dreaded "spaghetti":

<CodeBlock
  adapter="r"
  files={[
    {
      filename: "main.r",
      starterCode: `library(ggplot2)

# economics is a time series shipped with ggplot2: unemployment over time.
ggplot(economics, aes(x = date, y = unemploy)) +
  geom_line(color = "steelblue")
`,
    },
  ]}
/>

A line is right here because `date` is ordered. On `mpg`'s `displ`
(which has no inherent sequence between cars), a line would zig-zag
meaninglessly. **Choosing a geom is partly choosing a claim about your
data.**

<Callout type="info" title="Geoms vs. chart types">
In a menu-of-charts tool, "scatter plot" and "line chart" are separate
items. In ggplot2 they are the *same plot* with a different geom. This
is the grammar paying off: you stop memorizing chart types and start
swapping one component.
</Callout>

<MultipleChoice
  markdown={`What does a *geom* determine in a ggplot?

- Which data frame is plotted.
  > That is the *data* component, set in \`ggplot()\`.
- Which columns map to x, y, and color.
  > Those are *aesthetic mappings*, set in \`aes()\`.
- [o] The kind of mark used to represent the data — points, lines, bars, boxes, and so on.
  > The geom chooses *how* the mapped data is drawn.
- The fonts and background styling of the plot.
  > That is the *theme*.

The geom is the visible mark. Keeping data and mappings fixed while
swapping the geom yields different charts from one description.`}
/>

<MultipleChoice
  markdown={`Why is \`geom_line()\` appropriate for the \`economics\` data (unemployment over \`date\`) but a poor choice for \`mpg\` mapped as \`aes(displ, hwy)\`?

- \`geom_line()\` only works on data sets with more than 400 rows.
  > Row count is irrelevant; the issue is ordering.
- \`mpg\` has missing values that break lines.
  > That is not the reason; the reason is the absence of a meaningful order.
- [o] A line connects points in x order, which is meaningful for an ordered variable like \`date\` but produces meaningless zig-zags for an unordered variable like \`displ\`.
  > Lines encode sequence; only ordered x-values justify them.
- Lines cannot be colored, so they are unsuitable for \`mpg\`.
  > Lines can be colored; suitability is about ordering, not color.

A geom carries assumptions. Choosing \`geom_line\` asserts that the
x-values form a meaningful sequence.`}
/>

## Key takeaways

- A **geom** is the *mark* drawn — point, line, bar, box, tile.
- The **same data + mappings** can be drawn by many geoms; swapping the
  geom changes the chart without changing the description.
- Each geom has **required aesthetics** and carries **assumptions**
  (e.g. a line implies ordered x).
- "Scatter plot" vs. "line chart" is just a change of geom — the
  grammar replaces a menu of chart types with one swappable component.
