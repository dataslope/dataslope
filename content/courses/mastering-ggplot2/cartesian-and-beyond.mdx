---
title: Cartesian and Beyond
description: The coordinate system is the space marks live in — usually Cartesian, but the choice is a real grammar component with real consequences.
---

The **coordinate system** is the space your marks are placed in. You
have been using one all along — the standard Cartesian plane, x running
left-to-right and y bottom-to-top — without naming it. Making it an
explicit component unlocks zooming, aspect-ratio control, and (next
page) entirely different chart shapes.

<svg
  viewBox="0 0 640 220"
  role="img"
  aria-label="A faint Cartesian grid with axis arrows and one highlighted point dropping dotted projection lines to its x and y addresses"
  style={{ display: "block", width: "100%", maxWidth: "640px", height: "auto", margin: "2.5rem auto" }}
>
  <g fill="currentColor" opacity="0.08">
    <rect x="159" y="40" width="2" height="140" />
    <rect x="239" y="40" width="2" height="140" />
    <rect x="319" y="40" width="2" height="140" />
    <rect x="399" y="40" width="2" height="140" />
    <rect x="479" y="40" width="2" height="140" />
    <rect x="92" y="69" width="440" height="2" />
    <rect x="92" y="109" width="440" height="2" />
    <rect x="92" y="149" width="440" height="2" />
  </g>
  <g fill="currentColor" opacity="0.3">
    <rect x="88" y="180" width="452" height="4" rx="2" />
    <polygon points="540,174 540,190 558,182" />
    <rect x="88" y="38" width="4" height="146" rx="2" />
    <polygon points="82,42 98,42 90,26" />
  </g>
  <g fill="currentColor" opacity="0.35">
    <circle cx="400" cy="96" r="2.5" />
    <circle cx="400" cy="122" r="2.5" />
    <circle cx="400" cy="148" r="2.5" />
    <circle cx="364" cy="70" r="2.5" />
    <circle cx="328" cy="70" r="2.5" />
    <circle cx="292" cy="70" r="2.5" />
    <circle cx="256" cy="70" r="2.5" />
    <circle cx="220" cy="70" r="2.5" />
    <circle cx="184" cy="70" r="2.5" />
    <circle cx="148" cy="70" r="2.5" />
    <circle cx="112" cy="70" r="2.5" />
  </g>
  <g style={{ color: "var(--ds-blue-500)" }} fill="currentColor">
    <circle cx="400" cy="70" r="16" fillOpacity="0.15" />
    <circle cx="400" cy="70" r="9" />
    <circle cx="400" cy="182" r="5" />
    <circle cx="90" cy="70" r="5" />
  </g>
</svg>

## The default: Cartesian

`coord_cartesian()` is the familiar flat plane. It is applied
automatically, so these two are identical:

<CodeBlock
  adapter="r"
  files={[
    {
      filename: "main.r",
      starterCode: `library(ggplot2)

# The coordinate system is implicit here...
ggplot(mpg, aes(displ, hwy)) +
  geom_point()
`,
    },
  ]}
/>

<CodeBlock
  adapter="r"
  files={[
    {
      filename: "main.r",
      starterCode: `library(ggplot2)

# ...and explicit here. Same plot.
ggplot(mpg, aes(displ, hwy)) +
  geom_point() +
  coord_cartesian()
`,
    },
  ]}
/>

## Coordinates transform the *space*, not the data

Here is the key mental model. Scales and stats operate in **data
space** — they change values. Coordinates operate on the **final
space** — they reshape where already-computed marks land, *after* the
stats and scales have run.

The pipeline runs in this order: **Data → Stat → Scale** (all in data
space) **→ Coordinate system** (arranges the final space) **→ rendered
marks**.

This ordering is exactly why `coord_cartesian(xlim = ...)` *zooms*
rather than *filters*: the coordinate step happens last, after the
smoother has already been fit on all the data. (We met this on the
Position Scales page — now you can see *why*.)

<CodeBlock
  adapter="r"
  files={[
    {
      filename: "main.r",
      starterCode: `library(ggplot2)

# Zoom into a region with all data still used by the smoother.
ggplot(mpg, aes(displ, hwy)) +
  geom_point() +
  geom_smooth() +
  coord_cartesian(xlim = c(2, 5), ylim = c(20, 35))
`,
    },
  ]}
/>

## Fixing the aspect ratio

`coord_fixed()` forces one data unit on x to equal one data unit on y —
essential when x and y are in the **same units** and distances should
be comparable (maps, before/after on the same measurement scale):

<CodeBlock
  adapter="r"
  files={[
    {
      filename: "main.r",
      starterCode: `library(ggplot2)

# city vs highway MPG are the same units; fix the ratio so the
# y = x reference line is a true 45-degree diagonal.
ggplot(mpg, aes(cty, hwy)) +
  geom_point(alpha = 0.5) +
  geom_abline(slope = 1, intercept = 0, color = "red") +
  coord_fixed()
`,
    },
  ]}
/>

With `coord_fixed()`, the red `y = x` line is a genuine 45° diagonal,
so you can read "highway beats city MPG" as *every point above the
line*. Without it, the plot would stretch to fill the panel and distort
that comparison.

<Callout type="info" title="Coordinates are a full grammar component">
It is tempting to ignore coordinates because the default is almost
always right. But treating them as a real, swappable component is what
lets the *same* bar chart become a pie chart, or a Cartesian plot
become a map projection.
</Callout>

<MultipleChoice
  markdown={`In the ggplot pipeline, when does the coordinate system act relative to stats and scales?

- Before the stat, so it can change which rows are counted.
  > Coordinates act on the final arranged space, not on the raw data the stat sees.
- [o] After stats and scales, arranging the already-computed marks in the final space — which is why \`coord_cartesian(xlim=)\` zooms instead of filtering.
  > Coordinates reshape the final space last, so no data is dropped.
- At the same time as the mapping.
  > Mapping happens up front; coordinates come at the end.
- Coordinates never affect a plot; they are decorative.
  > They affect zoom, aspect ratio, and the very shape of the plot.

Because the coordinate step is last, it changes the *view* of computed
marks rather than the data feeding the stats.`}
/>

<MultipleChoice
  markdown={`Why might you add \`coord_fixed()\` to a plot of city MPG vs. highway MPG?

- To filter out cars with low MPG.
  > \`coord_fixed\` changes the aspect ratio, not the data.
- To convert the axes to a log scale.
  > That is a scale transformation, unrelated to \`coord_fixed\`.
- [o] Because both axes are in the same units, so equal data distances should look equal — making a \`y = x\` reference line a true 45-degree diagonal.
  > Fixing the ratio keeps same-unit comparisons visually honest.
- To add a confidence band to the points.
  > Bands come from \`geom_smooth\`, not from coordinates.

When x and y share units, a fixed coordinate ratio prevents the panel
from distorting the comparison between them.`}
/>

## Key takeaways

- The **coordinate system** is the space marks occupy; Cartesian is the
  default (`coord_cartesian()`).
- Coordinates act **last**, on the final space — so
  `coord_cartesian(xlim=)` **zooms** without discarding data.
- `coord_fixed()` enforces an equal x/y unit ratio, vital when the axes
  share units.
- Coordinates are a genuine, swappable component — the foundation for
  the polar and flipped charts on the next page.
