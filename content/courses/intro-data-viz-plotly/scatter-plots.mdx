---
title: Scatter Plots
description: The chart for seeing the relationship between two variables, and many extensions of it
---

A **scatter plot** maps two quantitative variables to the x and y
axes and draws one dot per row. It is, on the perceptual ranking
we met earlier, the *most powerful chart in the world*: both axes
use the most accurate visual encoding (position), so the eye can
read correlation, clusters, outliers, and shape at a glance.

<Figure
  slug="viz-scatter-plots-cutout"
  alt="Scatter plots, an isometric illustration"
/>

If you forget every other chart, remember the scatter plot.

## When to use a scatter plot

- You want to see **relationship** between two quantitative
  variables: correlated? non-linear? clustered?
- You want to spot **outliers**, points far from everyone else.
- You want to compare *individuals* (one dot per row), not
  aggregates.
- You'll later add a **third or fourth variable** through color or
  size (which makes it a *bubble chart*, its own page later).

What each strength of relationship actually looks like, since the number is
far less intuitive than people expect:

<Chart slug="correlation-scatters" />

## The minimum

We'll use the **Palmer Penguins** dataset, 344 Antarctic penguins
with bill, flipper, and body-mass measurements. It is the field's
favored friendly stand-in for the old "iris" flower dataset, whose
author, R.A. Fisher, was a prominent eugenicist; penguins teach the
same lessons without that baggage.

<CodeBlock
  adapter="python"
  datasets={[{ path: "palmer-penguins.csv", stageAs: "penguins.csv" }]}
  files={[
    {
      filename: "main.py",
      initCode: `import pandas as pd
import plotly.express as px

penguins = pd.read_csv("penguins.csv")`,
      starterCode: `fig = px.scatter(
    penguins,
    x="bill_depth_mm",
    y="bill_length_mm",
    template="simple_white",
    title="Penguins: bill depth vs bill length",
    labels={"bill_depth_mm": "Bill depth (mm)", "bill_length_mm": "Bill length (mm)"},
)
fig.show()
`,
    },
  ]}
/>

The picture answers "are these two measurements related?" in
about half a second.

## Adding categorical color

Color reveals *groups*. The penguin scatter becomes much richer once
we color by species:

<CodeBlock
  adapter="python"
  datasets={[{ path: "palmer-penguins.csv", stageAs: "penguins.csv" }]}
  files={[
    {
      filename: "main.py",
      initCode: `import pandas as pd
import plotly.express as px

penguins = pd.read_csv("penguins.csv")`,
      starterCode: `fig = px.scatter(
    penguins,
    x="bill_depth_mm",
    y="bill_length_mm",
    color="species",
    template="simple_white",
    title="Penguins by species",
    labels={"bill_depth_mm": "Bill depth (mm)", "bill_length_mm": "Bill length (mm)"},
)
fig.show()
`,
    },
  ]}
/>

Suddenly the structure becomes obvious: the three species occupy
different regions of the chart. (There's even a hint of **Simpson's
paradox** here, bill depth and length look negatively related
overall but positively related *within* each species.) *That's the
magic of adding the third variable.*

## Adding size for a fourth variable

A scatter where the size of each dot encodes another quantitative
variable is sometimes called a **bubble chart** (we'll dedicate a
whole page to it later):

<CodeBlock
  adapter="python"
  datasets={[{ path: "palmer-penguins.csv", stageAs: "penguins.csv" }]}
  files={[
    {
      filename: "main.py",
      initCode: `import pandas as pd
import plotly.express as px

# size= can't handle missing values, so drop rows with gaps.
penguins = pd.read_csv("penguins.csv").dropna(
    subset=["bill_depth_mm", "bill_length_mm", "body_mass_g"]
)`,
      starterCode: `fig = px.scatter(
    penguins,
    x="bill_depth_mm",
    y="bill_length_mm",
    color="species",
    size="body_mass_g",
    template="simple_white",
    title="Penguins with size encoding body mass",
    labels={"bill_depth_mm": "Bill depth (mm)", "bill_length_mm": "Bill length (mm)"},
)
fig.show()
`,
    },
  ]}
/>

Four variables on one chart, and still readable.

## Trendlines

When you want to summarize a relationship, add `trendline="ols"`
(ordinary least squares) or `trendline="lowess"` (a smoothed local
regression):

<CodeBlock
  adapter="python"
  files={[
    {
      filename: "main.py",
      starterCode: `import plotly.express as px

df = px.data.tips()

fig = px.scatter(
    df,
    x="total_bill",
    y="tip",
    color="sex",
    trendline="ols",
    template="simple_white",
    title="Tip vs total bill with linear trendlines per group",
    labels={"total_bill": "Total bill (USD)", "tip": "Tip (USD)"},
)
fig.show()
`,
    },
  ]}
/>

Trendlines turn a noisy cloud into a clear *direction*. Use them
when the question is "is there a relationship?" and the cloud is
busy.

The catch is that `"ols"` always succeeds, including when a straight
line is the wrong shape. The check is whether the misses form a
pattern:

<Chart slug="trendline-wrong-shape" />

## Overplotting: when there are too many points

Opacity is the cheapest fix and usually the only one you need. Same points,
same axes, one parameter changed:

<Chart slug="overplotting-alpha" />


Scatter plots can break down when you have **many thousands** of
points, they pile on top of each other and the dense regions all
look equally black. Two fixes:

### Opacity

The **diamonds** dataset, 53,940 rows of carat, cut, and price, is a
realistic stress test. At full opacity the cloud is a solid blob; at
`opacity=0.2` the dense regions reveal themselves as darker.

<CodeBlock
  adapter="python"
  datasets={[{ path: "diamonds.csv", stageAs: "diamonds.csv" }]}
  files={[
    {
      filename: "main.py",
      initCode: `import pandas as pd
import plotly.express as px

# 53,940 diamonds, far more points than a scatter can show as dots.
diamonds = pd.read_csv("diamonds.csv")`,
      starterCode: `fig = px.scatter(
    diamonds,
    x="carat",
    y="price",
    opacity=0.2,
    template="simple_white",
    title="Diamonds: carat vs price (53,940 points, opacity=0.2)",
    labels={"carat": "Carat", "price": "Price (USD)"},
)
fig.show()
`,
    },
  ]}
/>

Each dot is now semi-transparent, so dense regions become visibly
*darker*, the bands where most diamonds cluster jump out.

### Density heatmap

For really dense data, switch to a **density heatmap** (which we'll
meet in its own chapter), where the chart shows *how many points*
are in each region of the plane.

A trend line is a summary, and it hides as much as any other summary. The
worst case is not a line of the wrong shape, it is a line fitted where there
is nothing to fit:

<Chart slug="trendline-through-empty-space" />

## A typical scatter-plot mistake

Drawing a scatter plot with one *categorical* axis is usually not
what you want, it produces tall stacks of dots above each category
label. Use a **strip plot** (`px.strip`), **violin plot**
(`px.violin`), or **box plot** (`px.box`) instead, we'll cover
boxes next.

<Figure
  slug="viz-scatter-plots-inline-cutout"
  alt="A plain board with chips pinned at different heights and distances, forming a loose upward drift from left to right"
/>

The other one is quieter and harder to undo: plotting a
`groupby().mean()` and then reading it as if it described
individuals.

<Chart slug="scatter-mistake-aggregate" />

## Practice

<ChallengeCard
  adapter="python"
  datasets={[{ path: "palmer-penguins.csv", stageAs: "penguins.csv" }]}
  title="Scatter flipper length vs body mass"
  instructions={`The \`penguins\` DataFrame is loaded. Make a scatter plot that shows the relationship between flipper length and body mass, colored by species.

1. Use \`px.scatter\` with \`x="flipper_length_mm"\` and \`y="body_mass_g"\`.
2. Color the points by \`species\`.
3. Use \`template="simple_white"\`.

Store the figure in \`fig\`. (You should see a clear positive relationship, bigger flippers, heavier penguins.)`}
  files={[
    {
      filename: "main.py",
      initCode: `import pandas as pd
import plotly.express as px
penguins = pd.read_csv("penguins.csv")`,
      starterCode: `# Scatter: flipper length (x) vs body mass (y), colored by species.
fig = None
`,
      solutionCode: `fig = px.scatter(
    penguins,
    x="flipper_length_mm",
    y="body_mass_g",
    color="species",
    template="simple_white",
    title="Flipper length vs body mass, by species",
    labels={
        "flipper_length_mm": "Flipper length (mm)",
        "body_mass_g": "Body mass (g)",
    },
)
fig.show()
`,
    },
  ]}
  tests={[
    {
      id: "is-scatter",
      name: "fig is a scatter plot",
      description: "A scatter figure was built",
      code: `import plotly.graph_objects as go
assert isinstance(fig, go.Figure)
assert len(fig.data) >= 1
assert all(t.type == "scatter" and (t.mode is None or "markers" in t.mode) for t in fig.data)`,
    },
    {
      id: "by-species",
      name: "one trace per species",
      description: "color=species splits into three traces",
      code: `assert len(fig.data) == penguins["species"].nunique()`,
    },
    {
      id: "axes",
      name: "axes are flipper length and body mass",
      description: "x and y map to the right columns",
      code: `assert "flipper" in (fig.layout.xaxis.title.text or "").lower()
assert "mass" in (fig.layout.yaxis.title.text or "").lower() or "body" in (fig.layout.yaxis.title.text or "").lower()`,
    },
  ]}
/>

## Check your understanding

<MultipleChoice
  markdown={`What is a scatter plot best at showing?

- The composition of categorical groups.
  > Composition of categorical groups, how a total breaks down into named parts, is the job of bar charts, stacked bars, or pie charts. A scatter plot requires at least one quantitative axis and is not designed to show how groups sum to a whole.
- [o] The relationship between two quantitative variables, correlation, clusters, and outliers.
  > Scatter plots use position on both axes, the most accurate visual encoding. They reveal the *shape* of a two-variable relationship, linear? non-linear? clustered?, at a glance.
- A part-of-a-whole breakdown.
  > Part-of-a-whole breakdowns are the domain of pie charts, stacked bar charts, or treemaps, charts that show how individual components sum to a total. A scatter plot places individual observations in a 2-D space to reveal relationships, not compositions.
- A trend across regular time intervals.
  > Time-series trends with regular intervals are best shown with a line chart, which connects ordered time points with a continuous line to emphasize flow and direction. A scatter plot does not connect points and is suited to unordered x-y relationships.

Scatter plots are the workhorse of exploratory analysis. When in doubt about how two numeric variables relate, scatter them.`}
/>

<MultipleChoice
  markdown={`You have a scatter plot of 50,000 data points and everything looks like a black blob. What is a sensible fix?

- [o] Lower the marker opacity (e.g., **opacity=0.3**) or switch to a 2-D density heatmap.
  > Overplotting hides structure. Lowering opacity lets dense regions render darker, revealing where the points pile up. For very dense data, a **density_heatmap** or **density_contour** chart represents counts rather than individual points.
- Hide the axis labels.
  > Axis labels tell the viewer what each axis represents. Removing them makes the chart harder to interpret, not easier. The overplotting problem is in the data layer, not the labeling layer.
- Increase the marker size.
  > Increasing marker size makes overplotting worse: larger dots overlap even more, obscuring even more of the underlying data structure. The opposite, smaller, more transparent dots, helps with dense data.
- Switch to a pie chart.
  > Pie charts show parts of a whole for a small number of categories. A 50,000-row dataset with two quantitative variables is not suitable for a pie chart at all, it has no concept of individual observations in a 2-D space.

Visualization is the art of *seeing structure*. When the chart hides it, change the encoding.`}
/>

<MultipleChoice
  markdown={`What is a **trendline** on a scatter plot useful for?

- [o] It summarizes the central tendency of the relationship between x and y, making it easier to see direction (positive, negative, none) and to compare it across groups.
  > A noisy cloud of points can hide the underlying trend. A regression trendline (**trendline="ols"** for linear, **trendline="lowess"** for smoothed) extracts the central tendency. Just be sure to ask whether a *linear* line is appropriate before claiming the relationship is linear.
- It changes the underlying data.
  > A trendline is a visual overlay, it does not modify the DataFrame or the underlying data in any way. The original points remain unchanged; the line is a computed summary drawn on top of them.
- It removes outliers.
  > A trendline is fitted to all the data, including outliers, in fact, outliers can strongly influence the slope of an OLS line. Removing outliers is a separate data-cleaning step that must be done deliberately, not a side effect of adding a trendline.
- It applies a database join.
  > Database joins combine rows from different tables based on matching keys. A trendline is a statistical model fitted to the existing x-y data in the chart; it has nothing to do with database operations.

Trendlines are a *summary* layered on top of the data. Always show the underlying points too, the dispersion of the data is part of the story.`}
/>
