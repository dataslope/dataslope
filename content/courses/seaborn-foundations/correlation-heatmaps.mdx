---
title: Correlation Heatmaps
description: sns.heatmap on a correlation matrix, seeing every pairwise linear relationship at once, with the right colors.
---

A scatter plot compares *two* variables. But a dataset with six numeric
columns has fifteen pairs, far too many scatter plots to scan one at a
time. A **heatmap** collapses all of those pairwise relationships into a
single colored grid you can read in one glance.

`sns.heatmap` takes a **2-D matrix** of numbers and draws each cell as a
colored square, with the color encoding the cell's value. Its most common
job by far is rendering a **correlation matrix**, a square table whose
cell at row *i*, column *j* is the correlation between variable *i* and
variable *j*.

<Figure
  slug="sns-heatmaps-cutout"
  alt="A square grid of coloured cells on a platform running from one brand hue through neutral to another, a ramp bar beside it"
/>

## Building a correlation matrix

A correlation coefficient (Pearson's *r*) measures how tightly two numeric
variables move together on a scale from **−1 to +1**: +1 is a perfect
upward line, −1 a perfect downward line, and 0 no linear relationship.
pandas computes the whole matrix at once with `.corr(numeric_only=True)`
(the flag tells it to skip non-numeric columns rather than error on them):

<CodeBlock
  adapter="python"
  datasets={[{ path: "palmer-penguins.csv", stageAs: "penguins.csv" }]}
  files={[
    {
      filename: "main.py",
      starterCode: `import pandas as pd

penguins = pd.read_csv("penguins.csv")

# A square matrix: every numeric column correlated with every other.
corr = penguins.corr(numeric_only=True)
display(corr.round(2))
`,
    },
  ]}
/>

The result is square and symmetric, with **1.0 down the diagonal** (every
variable correlates perfectly with itself). Now hand it to `heatmap` and
turn on `annot=True` to print each number inside its cell:

<CodeBlock
  adapter="python"
  datasets={[{ path: "palmer-penguins.csv", stageAs: "penguins.csv" }]}
  files={[
    {
      filename: "main.py",
      starterCode: `import pandas as pd
import seaborn as sns

sns.set_theme()
penguins = pd.read_csv("penguins.csv")
corr = penguins.corr(numeric_only=True)

# annot=True writes the correlation value into each cell.
sns.heatmap(corr, annot=True)
`,
    },
  ]}
/>

To read it: pick a row and a column, find where they cross, and that cell's
color and number are their correlation. `flipper_length_mm` and
`body_mass_g` light up strongly, long-flippered penguins are heavier,
while `bill_depth_mm` runs *negative* against the size variables.

## The key lesson: choose a colormap that matches the data

A heatmap's whole message is carried by **color**, so the colormap is not
decoration, it is the encoding. And correlation is a special kind of
number: it is **signed**, running from −1 to +1, with **0 as a meaningful
midpoint** that means "no linear relationship."

That demands a **diverging** colormap: two contrasting hues meeting at a
neutral color in the middle. Center it at zero and fix the limits to the
full range so the colors are anchored to their true meaning.

The same correlation matrix twice, once on a rainbow ramp and once on a
diverging one centred at zero:

<Chart slug="rainbow-vs-sequential" />

A correlation matrix is also worth reading as a whole rather than cell by
cell. Sorted so related variables sit together, the blocks are the story:

<Chart slug="correlation-matrix" />

<CodeBlock
  adapter="python"
  datasets={[{ path: "palmer-penguins.csv", stageAs: "penguins.csv" }]}
  files={[
    {
      filename: "main.py",
      starterCode: `import pandas as pd
import seaborn as sns

sns.set_theme()
penguins = pd.read_csv("penguins.csv")
corr = penguins.corr(numeric_only=True)

# Diverging map centered at 0, scaled to the full -1..+1 range.
sns.heatmap(corr, annot=True, cmap="vlag", center=0, vmin=-1, vmax=1)
`,
    },
  ]}
/>

Now the colors *mean* something consistent: one hue for positive, the other
for negative, pale neutral at zero, and saturation growing toward each
extreme. A strong negative and a strong positive correlation look clearly
*different*, not merely "light" versus "dark."

<Callout type="danger" title="Why a sequential colormap misleads here">
A **sequential** map (like `"viridis"` or `"Blues"`) runs from low to high
along a *single* direction, it is built for data with a natural floor, such
as counts or magnitudes. Drop a correlation matrix onto it and −0.9 and
+0.9, opposite relationships, get pushed to *opposite ends of one ramp*,
as if −0.9 were simply "less" than +0.9. The crucial sign distinction, and
the special status of zero, both vanish. Signed data needs a diverging map
centered at its midpoint. (We dig much deeper into choosing palettes in the
color-palettes chapter.)
</Callout>

So the reading rules for a correlation heatmap are:

- **Saturation / brightness** → *strength* (pale ≈ 0, vivid ≈ ±1).
- **Hue (which color)** → *direction* (one color positive, the other
  negative).
- **The diagonal is always 1.0**, every variable with itself, so ignore
  it; the story is in the off-diagonal cells.

<MultipleChoice
  markdown={`You are drawing a heatmap of a correlation matrix (values from −1 to +1).
Which colormap is the right choice, and why?

- Any colormap works; the annotations carry the meaning, so color is just decoration.
  > Color *is* the encoding in a heatmap, it is what lets you scan the whole grid at a glance. If you only trust the numbers, you have given up the heatmap's main advantage.
- [o] A diverging map like \`"vlag"\` centered at 0, because correlation is signed with a meaningful zero midpoint.
  > Diverging maps use two hues meeting at a neutral middle, so positive and negative correlations read as genuinely different directions and zero is visibly neutral.
- A sequential map like \`"viridis"\`, because it looks modern and is easy to read.
  > A sequential ramp encodes a single low-to-high direction, so it would place −0.9 and +0.9 at opposite ends as if one were merely "less" than the other, erasing the sign.
- A reversed sequential map, so high values are dark.
  > Reversing direction still leaves a single-direction ramp; it cannot represent a midpoint or distinguish the two signs the way a diverging map does.

Signed data with a meaningful midpoint calls for a diverging colormap
centered on that midpoint.`}
/>

## A second use: heatmaps of a pivot table

Correlation is the famous case, but `heatmap` will color *any* 2-D matrix
where both axes are meaningful. A natural source is a **pivot table**:
reshape a tidy table into a grid and color the cells. The `flights` data,
monthly airline passengers over a span of years, is perfect:

<CodeBlock
  adapter="python"
  files={[
    {
      filename: "main.py",
      starterCode: `import seaborn as sns

sns.set_theme()
flights = sns.load_dataset("flights")

# Reshape tidy/long data into a months x years matrix.
matrix = flights.pivot(index="month", columns="year", values="passengers")
sns.heatmap(matrix)
`,
    },
  ]}
/>

Two patterns jump out of the colors immediately: every column gets brighter
from bottom to top (passenger numbers grew year over year), and the same
summer rows stay bright across *every* year (a seasonal travel peak). That
is two-dimensional structure you would struggle to see in a table of raw
numbers.

<Callout type="info" title="Heatmaps want WIDE / matrix data">
Notice the input shape. Most Seaborn plots want **tidy/long** data, but a
heatmap is the exception we flagged back on the tidy-data page: it needs a
**wide matrix**, because its two axes *are* the row and column labels and
each cell *is* a value. `pivot` (the reverse of `melt`) is how you reshape
long data into that grid. Passenger counts here are non-negative magnitudes,
so the default **sequential** colormap is the correct choice, sequential
for one-directional magnitudes, diverging only for signed data like
correlations.
</Callout>

## Pitfalls

<Callout type="danger" title="Correlation only sees STRAIGHT-line relationships">
Pearson's *r* measures **linear** association only. A relationship can be
strong, clean, and obvious to the eye yet have *r* ≈ 0, for example a
perfect U-shape ($y = x^2$), where the upward and downward halves cancel out.
This is the heatmap's blind spot: a near-zero, pale cell does **not** mean
"no relationship," only "no *straight-line* relationship." (Remember
Anscombe's quartet: wildly different scatters can share the same
correlation.) A heatmap is a fast triage tool, confirm anything
interesting with an actual scatter plot.
</Callout>

A second, practical pitfall: **a huge matrix is unreadable.** Thirty
variables make a 30×30 grid of 900 tiny cells, far too dense to scan, and
the annotations become unreadable. Two fixes:

- **Select the variables that matter** before computing `.corr()`, rather
  than dumping every column in.
- **Mask the redundant half.** A correlation matrix is symmetric, so the
  upper and lower triangles are mirror images, half the grid is repeated.
  You can hide the upper triangle by passing a boolean `mask` built with
  NumPy's `np.triu` (the **tri**angle, **u**pper). Cells where the mask is
  `True` are left blank:

<CodeBlock
  adapter="python"
  datasets={[{ path: "palmer-penguins.csv", stageAs: "penguins.csv" }]}
  files={[
    {
      filename: "main.py",
      starterCode: `import pandas as pd
import seaborn as sns
import numpy as np

sns.set_theme()
penguins = pd.read_csv("penguins.csv")
corr = penguins.corr(numeric_only=True)

# True for the upper triangle (incl. diagonal) -> those cells stay blank.
mask = np.triu(np.ones_like(corr, dtype=bool))

sns.heatmap(corr, mask=mask, annot=True, cmap="vlag", center=0, vmin=-1, vmax=1)
`,
    },
  ]}
/>

With the duplicated half hidden, each pair appears exactly once and the eye
has far less to wade through, a small change that makes a large matrix
legible.

## Your turn

<ChallengeCard
  adapter="python"
  title="A correlation heatmap with the right colors"
  instructions={`Using the \`mpg\` dataset:

1. Compute its correlation matrix with \`mpg.corr(numeric_only=True)\` and
   store it in \`corr\`.
2. Draw an **annotated** heatmap of \`corr\` using a **diverging** colormap
   (e.g. \`cmap="vlag"\`) **centered at 0**.
3. Assign the Axes that \`sns.heatmap\` returns to a variable named \`ax\`
   (i.e. \`ax = sns.heatmap(...)\`).`}
  files={[
    {
      filename: "main.py",
      starterCode: `import seaborn as sns

sns.set_theme()
mpg = sns.load_dataset("mpg")

# 1) Build the correlation matrix:
corr = None

# 2) + 3) Draw the annotated, diverging, zero-centered heatmap and keep the Axes:
ax = None
`,
      solutionCode: `import seaborn as sns

sns.set_theme()
mpg = sns.load_dataset("mpg")

corr = mpg.corr(numeric_only=True)

ax = sns.heatmap(corr, annot=True, cmap="vlag", center=0, vmin=-1, vmax=1)
`,
    },
  ]}
  tests={[
    {
      id: "corr_square",
      name: "corr is a square matrix",
      description: "Same number of rows and columns",
      code: "assert corr.shape == (corr.shape[0], corr.shape[0]), f'A correlation matrix should be square; got {corr.shape}'",
    },
    {
      id: "diagonal_ones",
      name: "The diagonal is all 1.0",
      description: "Each variable correlates 1.0 with itself",
      code: "import numpy as np\nvals = np.diag(corr.to_numpy())\nassert all(round(float(v), 5) == 1.0 for v in vals), 'The diagonal of a correlation matrix must be all 1.0'",
    },
    {
      id: "is_axes",
      name: "ax is a matplotlib Axes",
      description: "heatmap returns the Axes it drew on",
      code: "import matplotlib\nassert isinstance(ax, matplotlib.axes.Axes), 'Assign the result of sns.heatmap(...) to ax'",
    },
  ]}
/>

## Check your understanding

<MultipleChoice
  markdown={`Why is a **diverging** colormap (centered at 0) the right choice for a
correlation heatmap?

- [o] Because correlation is signed (−1 to +1) with 0 as a meaningful midpoint, and a diverging map represents both direction and the neutral center.
  > Centering a two-hue map at zero makes positive and negative correlations read as opposite directions, with pale neutral marking "no linear relationship."
- Because Seaborn refuses to draw a correlation matrix with a sequential map.
  > Seaborn will happily use a sequential map, it just *misleads*, collapsing the sign distinction. The choice is yours to make correctly.
- Because diverging colormaps have more colors, so they show more detail.
  > It is not about the *number* of colors. It is that two hues meeting at a neutral midpoint match data that has a meaningful center and runs in two directions.
- Because diverging colormaps are colorblind-safe and sequential ones never are.
  > Colorblind-safety depends on the specific palette, not on whether it is diverging or sequential; that is a separate concern from matching signed data.

A diverging, zero-centered map encodes both the sign and the strength of
correlation faithfully.`}
/>

<MultipleChoice
  markdown={`A cell in your correlation heatmap is pale and shows *r* ≈ 0.02. What is the
safest interpretation?

- The colormap is wrong and is hiding the true value.
  > The annotation already shows the value; a pale cell faithfully represents *r* near zero. The limitation is what *r* measures, not how it is colored.
- The data must contain an error, because real variables are always correlated.
  > Genuinely unrelated variables exist, and so do nonlinear ones; a near-zero coefficient is not evidence of a data error.
- The two variables are completely unrelated.
  > Pearson's *r* only sees *linear* association. A strong curved relationship (such as a U-shape) can produce *r* ≈ 0 while the variables are tightly related.
- [o] There is little *linear* relationship; a curved relationship could still exist, so check a scatter plot.
  > Near-zero *r* rules out a straight-line trend, not every relationship. A scatter plot reveals curvature that correlation cannot.

Correlation captures only straight-line association, so a near-zero cell
warrants a look at the actual scatter.`}
/>

<MultipleChoice
  markdown={`Your dataset has 25 numeric columns and the resulting 25×25 heatmap is an
illegible wall of tiny cells. What is a reasonable fix?

- Make the figure smaller so it all fits on screen at once.
  > Shrinking the figure makes each cell *smaller*, worsening the legibility problem rather than relieving it.
- Switch to a sequential colormap to compress the range.
  > A colormap change affects how values are *colored*, not how many cells there are; the matrix is still 25×25 and unreadable.
- [o] Select the variables you actually care about before computing \`.corr()\`, and/or mask the redundant upper triangle.
  > Fewer, chosen variables yield a smaller grid, and masking the mirror-image half removes duplicated cells, both make a dense matrix readable.
- Turn off \`annot\` and rely on guessing the values from color.
  > Removing the numbers may de-clutter slightly, but 625 cells are still too many to scan; the grid is too large regardless of annotations.

For an overcrowded matrix, reduce what you plot, pick variables and/or mask
the symmetric half.`}
/>

You can now read every pairwise *linear* relationship in a dataset from a
single grid, and you know exactly where that view goes blind. Next we keep
exploring many variables together with **pairwise relationship** plots,
which restore the scatter that the heatmap had to leave out.
