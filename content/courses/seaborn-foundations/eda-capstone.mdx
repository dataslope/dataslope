---
title: "Capstone: A Full Exploration"
description: "Put it all together, explore the penguins dataset end to end, from first glance to a communication-ready figure."
---

Every chapter so far taught one chart family in isolation. Real
**exploratory data analysis (EDA)** does not work one chart at a time, it
is a *loop*. You glance at the raw table, ask a question, draw the chart
that answers it, notice something new, and ask the next question. The skill
is not knowing twenty chart types; it is knowing which one to reach for at
each step, and reading what it reveals before moving on.

This page is a full pass through that loop on the **penguins** dataset,
344 penguins measured across three species and three islands. We'll go from
"what is even in this table?" to a single, polished figure you could drop
into a report. Along the way you will write a lot of the code yourself.

<Figure
  slug="sns-eda-capstone-cutout"
  alt="A small penguin figure standing on a platform beside a grid of six chart panels, one panel highlighted"
/>

<Callout type="info" title="The EDA loop, in one breath">
**Glance** at the data, look at **one variable** at a time, **compare
groups**, study **relationships** between pairs, then zoom out to **all
pairs at once**, and finally turn your best finding into a figure that
*communicates*. Each step narrows the question for the next.
</Callout>

## Step 1, First glance

Before any chart, look at the table itself. Three quick checks answer
"what am I working with?": the **shape** (how many rows and columns), the
**head** (what the columns and values actually look like), and the
**missing values** (where the holes are). Skipping this step is how people
end up plotting a column that is half empty and drawing the wrong
conclusion.

<CodeBlock
  adapter="python"
  datasets={[{ path: "palmer-penguins.csv", stageAs: "penguins.csv" }]}
  files={[
    {
      filename: "main.py",
      starterCode: `import pandas as pd

penguins = pd.read_csv("penguins.csv")

# How big is it? (rows, columns)
print("shape:", penguins.shape)

# What do the columns and first rows look like?
print(penguins.head())

# Where are the missing values?
print(penguins.isna().sum())
`,
    },
  ]}
/>

Read the output top to bottom. The **shape** is `(344, 7)`: 344 penguins,
seven columns. The **head** shows the columns split cleanly into *who the
penguin is* (`species`, `island`, `sex`, categorical labels) and *how it
was measured* (`bill_length_mm`, `bill_depth_mm`, `flipper_length_mm`,
`body_mass_g`, numbers). That split is the single most useful thing to
notice, because it tells you which charts are even possible: numeric
columns can go on a scatter plot's axes; categorical columns can color or
split it.

The **missing-value count** shows a handful of `NaN`s, two penguins are
missing every measurement, and a few more are missing `sex`. This is
normal, and Seaborn quietly drops missing rows per plot, so we can keep
moving. But now we *know* the holes are there instead of being surprised
later.

<Callout type="tip" title="Categorical vs numeric is the first fork">
The moment you can sort a table's columns into "labels" and "numbers" you
already know your options: a histogram or box plot for one numeric column,
a scatter for two numeric columns, `hue`/`col` for the categorical ones.
The data types drive the chart choice, not the other way around.
</Callout>

## Step 2, One variable at a time

With the lay of the land clear, start *narrow*: look at a single numeric
column on its own. A **histogram** (via `displot()` with `kind="hist"`)
turns one column of numbers into a shape, where values pile up, how spread
out they are, whether there is more than one peak.

<Figure
  slug="sns-eda-capstone-step-2-one-variable-inline-cutout"
  alt="A single row of marks tipped into a line of bins, settling into two humps rather than one"
  caption="Two humps rather than one is the first sign that the column holds more than one group."
/>

Here is `body_mass_g`, the penguins' weight:

<CodeBlock
  adapter="python"
  datasets={[{ path: "palmer-penguins.csv", stageAs: "penguins.csv" }]}
  files={[
    {
      filename: "main.py",
      starterCode: `import pandas as pd
import seaborn as sns

sns.set_theme(style="whitegrid")
penguins = pd.read_csv("penguins.csv")

# The distribution of one numeric column: where does weight pile up?
sns.displot(penguins, x="body_mass_g", kind="hist", bins=20, height=4)
`,
    },
  ]}
/>

The shape is not a single tidy hump. There is a big cluster of lighter
penguins on the left and a smaller, heavier group trailing off to the
right, a hint that *more than one kind of penguin* is mixed into this
column. That hint is exactly the kind of thing EDA exists to surface: a
single-variable plot raised a question ("who are the heavy ones?") that the
next steps will answer.

Now you try the same move on a different column, and split it by species
so the groups show up in color.

<ChallengeCard
  adapter="python"
  datasets={[{ path: "palmer-penguins.csv", stageAs: "penguins.csv" }]}
  title="Histogram of flipper length, colored by species"
  instructions={`Using the \`penguins\` dataset, draw a **histogram** of
\`flipper_length_mm\` with \`sns.displot()\`:

- put \`flipper_length_mm\` on the x-axis,
- use \`kind="hist"\`,
- color the bars by species with \`hue="species"\`.

Assign the result to a variable named \`g\`.`}
  files={[
    {
      filename: "main.py",
      starterCode: `import pandas as pd
import seaborn as sns

sns.set_theme(style="whitegrid")
penguins = pd.read_csv("penguins.csv")

# Build the histogram and assign it to g:
g = None
`,
      solutionCode: `import pandas as pd
import seaborn as sns

sns.set_theme(style="whitegrid")
penguins = pd.read_csv("penguins.csv")

g = sns.displot(
    penguins,
    x="flipper_length_mm",
    hue="species",
    kind="hist",
    height=4,
)
`,
    },
  ]}
  tests={[
    {
      id: "is_facetgrid",
      name: "Uses displot",
      description: "g is a Seaborn FacetGrid",
      code: "import seaborn as sns\nassert isinstance(g, sns.axisgrid.FacetGrid), 'Assign the result of sns.displot(...) to g'",
    },
    {
      id: "xlabel",
      name: "flipper_length_mm is on the x-axis",
      description: "g.ax.get_xlabel() == 'flipper_length_mm'",
      code: "assert g.ax.get_xlabel() == 'flipper_length_mm', f\"x should be flipper_length_mm, got {g.ax.get_xlabel()!r}\"",
    },
  ]}
/>

Running your version, the colors tell the story the body-mass histogram
only hinted at: the right-hand group is almost entirely one species. A
single variable, split by a category, already starts separating the
penguins into kinds.

## Step 3, Comparing groups

The histogram suggested the species differ. The natural next question is
*by how much, and on which measurements?* When one axis is a **category**
and the other is a **number**, that is a job for `catplot()`. A
**box plot** (`kind="box"`) summarizes each group's spread, the box is the
middle 50%, the line is the median, the whiskers reach most of the rest,
so you can line the groups up and compare centers and overlap at a glance.

<Figure
  slug="sns-eda-capstone-step-3-comparing-groups-inline-cutout"
  alt="Three columns of marks standing side by side, each with a box drawn round its middle half"
  caption="A box per category shows the middle half and makes the group differences comparable."
/>

Body mass by species:

<CodeBlock
  adapter="python"
  datasets={[{ path: "palmer-penguins.csv", stageAs: "penguins.csv" }]}
  files={[
    {
      filename: "main.py",
      starterCode: `import pandas as pd
import seaborn as sns

sns.set_theme(style="whitegrid")
penguins = pd.read_csv("penguins.csv")

# Category on x, number on y: compare each species' weight distribution.
sns.catplot(
    data=penguins,
    x="species",
    y="body_mass_g",
    kind="box",
    height=4,
)
`,
    },
  ]}
/>

Now the suspicion is confirmed and quantified: **Gentoo** penguins are
clearly heavier than **Adelie** and **Chinstrap**, whose boxes sit lower
and overlap each other heavily. The box plot earns its keep here, three
distributions compared in one glance, with medians and spread visible at
once. (Swap `kind="box"` for `kind="violin"` to see each group's full
density shape instead of a five-number summary.)

The other categorical column we noticed in Step 1 was `island`. Does weight
vary by *where* a penguin lives, the way it varies by species? Find out.

<ChallengeCard
  adapter="python"
  datasets={[{ path: "palmer-penguins.csv", stageAs: "penguins.csv" }]}
  title="Compare body mass across islands"
  instructions={`Using the \`penguins\` dataset, draw a categorical plot with
\`sns.catplot()\` comparing \`body_mass_g\` across islands:

- put \`island\` on the x-axis,
- put \`body_mass_g\` on the y-axis,
- use \`kind="box"\` (or \`kind="violin"\` if you prefer).

Assign the result to a variable named \`g\`.`}
  files={[
    {
      filename: "main.py",
      starterCode: `import pandas as pd
import seaborn as sns

sns.set_theme(style="whitegrid")
penguins = pd.read_csv("penguins.csv")

# Build the catplot and assign it to g:
g = None
`,
      solutionCode: `import pandas as pd
import seaborn as sns

sns.set_theme(style="whitegrid")
penguins = pd.read_csv("penguins.csv")

g = sns.catplot(
    data=penguins,
    x="island",
    y="body_mass_g",
    kind="box",
    height=4,
)
`,
    },
  ]}
  tests={[
    {
      id: "is_facetgrid",
      name: "Uses catplot",
      description: "g is a Seaborn FacetGrid",
      code: "import seaborn as sns\nassert isinstance(g, sns.axisgrid.FacetGrid), 'Assign the result of sns.catplot(...) to g'",
    },
    {
      id: "xlabel",
      name: "island is on the x-axis",
      description: "g.ax.get_xlabel() == 'island'",
      code: "assert g.ax.get_xlabel() == 'island', f\"x should be island, got {g.ax.get_xlabel()!r}\"",
    },
  ]}
/>

Your island plot shows that **Biscoe** runs heavier than the others, but
be careful with the *why*. Islands are not equally stocked with species
(Gentoo live only on Biscoe), so "Biscoe penguins are heavy" is really
"Gentoo live on Biscoe, and Gentoo are heavy." A grouping variable can be a
stand-in for a different cause. EDA is as much about catching these
confounds as about finding patterns.

## Step 4, Relationships between pairs

Single variables and group comparisons are warm-ups. The richest questions
in this dataset are about how two measurements move *together*. Back to the
**scatter plot** (`relplot()`), now armed with what we know, color by
species so the structure is unmistakable.

<Figure
  slug="sns-eda-capstone-step-4-relationships-between-inline-cutout"
  alt="Three separate runs of marks that each slope upward, with one line drawn across the whole set sloping down"
  caption="Every species trends upward, and pooling them produces a line that trends the other way."
/>

Flipper length against body mass:

<CodeBlock
  adapter="python"
  datasets={[{ path: "palmer-penguins.csv", stageAs: "penguins.csv" }]}
  files={[
    {
      filename: "main.py",
      starterCode: `import pandas as pd
import seaborn as sns

sns.set_theme(style="whitegrid")
penguins = pd.read_csv("penguins.csv")

# Two numbers on the axes; the third variable (species) as color.
sns.relplot(
    data=penguins,
    x="flipper_length_mm",
    y="body_mass_g",
    hue="species",
    height=4,
)
`,
    },
  ]}
/>

This is one of the cleanest relationships you will ever see: longer
flippers go with heavier bodies, tightly and almost linearly, and the three
species fall into their own regions of the cloud. Gentoo sit up and to the
right (long flippers, heavy); Adelie and Chinstrap overlap in the
lower-left. The scatter plot shows the *relationship*; `hue` shows that the
relationship is shared across groups that nonetheless occupy different
parts of it.

When a relationship looks linear, it is natural to ask for the *line*.
`lmplot()` draws a scatter plus a fitted regression line per group, a quick
way to compare trends. Try it on the two bill measurements, which (you may
recall from earlier) hide a surprise when you split by species.

<ChallengeCard
  adapter="python"
  datasets={[{ path: "palmer-penguins.csv", stageAs: "penguins.csv" }]}
  title="Regression lines for bill shape by species"
  instructions={`Using the \`penguins\` dataset, draw a regression plot with
\`sns.lmplot()\`:

- put \`bill_length_mm\` on the x-axis,
- put \`bill_depth_mm\` on the y-axis,
- color (and fit a separate line) by species with \`hue="species"\`.

Assign the result to a variable named \`g\`.`}
  files={[
    {
      filename: "main.py",
      starterCode: `import pandas as pd
import seaborn as sns

sns.set_theme(style="whitegrid")
penguins = pd.read_csv("penguins.csv")

# Build the lmplot and assign it to g:
g = None
`,
      solutionCode: `import pandas as pd
import seaborn as sns

sns.set_theme(style="whitegrid")
penguins = pd.read_csv("penguins.csv")

g = sns.lmplot(
    data=penguins,
    x="bill_length_mm",
    y="bill_depth_mm",
    hue="species",
    height=4,
)
`,
    },
  ]}
  tests={[
    {
      id: "is_facetgrid",
      name: "Uses lmplot",
      description: "g is a Seaborn FacetGrid",
      code: "import seaborn as sns\nassert isinstance(g, sns.axisgrid.FacetGrid), 'Assign the result of sns.lmplot(...) to g'",
    },
    {
      id: "xlabel",
      name: "bill_length_mm is on the x-axis",
      description: "g.ax.get_xlabel() == 'bill_length_mm'",
      code: "assert g.ax.get_xlabel() == 'bill_length_mm', f\"x should be bill_length_mm, got {g.ax.get_xlabel()!r}\"",
    },
    {
      id: "has_legend",
      name: "Colored by species",
      description: "A species legend is present",
      code: "assert g._legend is not None, 'Map hue=\"species\" so each species gets its own colored points and line'",
    },
  ]}
/>

Look at the three fitted lines. *Within* each species, longer bills are
also **deeper**, every line slopes upward. Yet if you erased the colors
and fitted one line to the whole cloud, the trend would look flat or even
*downward*, because Gentoo (long, shallow bills) pull the overall picture
sideways. That reversal, a within-group trend that vanishes or flips when
groups are pooled, is **Simpson's paradox**, and `lmplot()` with `hue` makes
it visible in a single chart.

## Step 5, All pairs at once

You have been picking pairs of columns by hand. With only four numeric
columns there are not that many pairs, so why not look at *all of them at
once?* A **pair plot** draws every numeric column against every other in a
grid, with each variable's own distribution down the diagonal. It is the
fastest way to scan an entire numeric dataset for structure.

<Figure
  slug="sns-eda-capstone-step-5-all-pairs-inline-cutout"
  alt="A square grid of small patches, pale along the diagonal and deepening toward two opposite hues at opposite corners"
  caption="A diverging palette pinned at zero keeps positive and negative correlations distinct."
/>

<CodeBlock
  adapter="python"
  datasets={[{ path: "palmer-penguins.csv", stageAs: "penguins.csv" }]}
  files={[
    {
      filename: "main.py",
      starterCode: `import pandas as pd
import seaborn as sns

sns.set_theme(style="whitegrid")
penguins = pd.read_csv("penguins.csv")

# Every numeric column vs every other, colored by species.
sns.pairplot(penguins, hue="species", height=1.8)
`,
    },
  ]}
/>

In one figure you can confirm everything the earlier steps found and spot
things you had not looked for: flipper-vs-mass is tight and linear; the bill
measurements separate the species into neat clusters; and the diagonal
shows how each measurement alone is split by species. A pair plot is busy,
but for a handful of numeric columns it is an unbeatable overview.

A complementary view of "everything at once" is the **correlation matrix**:
a single number per pair of columns summarizing how strongly they move
together, from `-1` (perfect opposite) through `0` (no linear relation) to
`+1` (perfect together). A **heatmap** turns that matrix into color.

<CodeBlock
  adapter="python"
  datasets={[{ path: "palmer-penguins.csv", stageAs: "penguins.csv" }]}
  files={[
    {
      filename: "main.py",
      starterCode: `import pandas as pd
import seaborn as sns

sns.set_theme(style="white")
penguins = pd.read_csv("penguins.csv")

# One number per pair: how strongly do the measurements move together?
corr = penguins.corr(numeric_only=True)

# A diverging palette centered at 0: red = negative, blue = positive.
sns.heatmap(corr, annot=True, cmap="vlag", center=0, square=True)
`,
    },
  ]}
/>

Two choices make this heatmap honest. The palette `"vlag"` is **diverging**,
it runs from one color through white to another, and `center=0` pins
white to zero, so positive correlations read as one hue and negative ones
as the opposite, with strength shown by intensity. A sequential palette, or
forgetting `center=0`, would make a `+0.1` and a `-0.1` look misleadingly
different in size. The bright cell between flipper length and body mass
(near `+0.87`) is the strong relationship you already saw as a scatter, now
quantified.

That `corr()` matrix has structure worth understanding directly, not just
coloring. Build it yourself and check two facts that are true of *every*
correlation matrix.

<ChallengeCard
  adapter="python"
  datasets={[{ path: "palmer-penguins.csv", stageAs: "penguins.csv" }]}
  title="Build and sanity-check a correlation matrix"
  instructions={`A **correlation matrix** is square (one row and one column per
numeric variable) and every variable correlates perfectly with itself, so
its diagonal is all \`1\`.

Using the \`penguins\` dataset, build the correlation matrix of its numeric
columns and assign it to a variable named \`corr()\`:

\`\`\`
corr = penguins.corr(numeric_only=True)
\`\`\`

The tests will confirm that \`corr()\` is square and that its top-left entry
(a variable's correlation with itself) is \`1\`.`}
  files={[
    {
      filename: "main.py",
      starterCode: `import pandas as pd

penguins = pd.read_csv("penguins.csv")

# Build the correlation matrix of the numeric columns:
corr = None
`,
      solutionCode: `import pandas as pd

penguins = pd.read_csv("penguins.csv")

corr = penguins.corr(numeric_only=True)
`,
    },
  ]}
  tests={[
    {
      id: "is_square",
      name: "corr is a square matrix",
      description: "Same number of rows and columns",
      code: "assert corr is not None, 'Assign penguins.corr(numeric_only=True) to corr'\nassert corr.shape[0] == corr.shape[1], f'A correlation matrix is square; got shape {corr.shape}'\nassert corr.shape[0] >= 2, f'Expected several numeric columns, got shape {corr.shape}'",
    },
    {
      id: "diagonal_is_one",
      name: "The diagonal is 1",
      description: "Every variable correlates 1.0 with itself",
      code: "assert round(float(corr.iloc[0, 0]), 3) == 1.0, f'A variable correlates perfectly with itself; got {corr.iloc[0, 0]!r} on the diagonal'",
    },
  ]}
/>

The diagonal being exactly `1` and the matrix being square are not
coincidences, they fall out of what correlation *is*. Knowing that lets
you read a heatmap critically: the diagonal carries no information (it is
always the brightest, "perfect" color), so your eye should go straight to
the **off-diagonal** cells where the real relationships live.

## Step 6, Communicate the finding

Exploration is for *you*; a final figure is for *someone else*. The last
move of an EDA is to take the single most important thing you found and
draw it so a reader gets it in seconds, with a **message-style title** that
states the takeaway, not just the variable names.

<Figure
  slug="sns-eda-capstone-step-6-communicate-finding-inline-cutout"
  alt="A plain scatter with one line, one note and one highlight added, the point now visible without hunting for it"
  caption="The same data, but the reader no longer has to derive the point for themselves."
/>

Our cleanest finding was that the species separate by **bill shape**. Here
is that scatter, dressed for an audience: a title that says what the chart
*means*, and clear axis labels.

<CodeBlock
  adapter="python"
  datasets={[{ path: "palmer-penguins.csv", stageAs: "penguins.csv" }]}
  files={[
    {
      filename: "main.py",
      starterCode: `import pandas as pd
import seaborn as sns

sns.set_theme(style="whitegrid", context="talk")
penguins = pd.read_csv("penguins.csv")

g = sns.relplot(
    data=penguins,
    x="bill_length_mm",
    y="bill_depth_mm",
    hue="species",
    height=5,
    aspect=1.2,
)

# Communication-ready labels.
g.set_axis_labels("Bill length (mm)", "Bill depth (mm)")
g.legend.set_title("Species")

# A title that states the TAKEAWAY, not just the variables.
g.figure.suptitle("Penguin species separate cleanly by bill shape")
# Make room for the title above the plot.
g.figure.subplots_adjust(top=0.9)
`,
    },
  ]}
/>

Compare this to the bare scatter from Step 4. The data is identical; what
changed is that a reader no longer has to *derive* the point, the title
hands it to them, and the labels read in plain English. That is the
difference between an **exploratory** figure (terse, fast, for yourself) and
an **explanatory** one (titled, labeled, for an audience). Both are
legitimate; knowing which you are making is the skill.

<Callout type="tip" title="Title with the verb, not the nouns">
"`bill_length_mm` vs `bill_depth_mm`" describes the axes. "Penguin species
separate cleanly by bill shape" describes the *finding*. For a figure meant
to persuade or inform, prefer the sentence with a verb in it, it tells the
reader what to take away before they finish reading the axes.
</Callout>

## Check your understanding

<MultipleChoice
  markdown={`You open a brand-new dataset you have never seen. Which step makes the most
sense **first**, before drawing any chart?

- Pick a color palette so your charts look polished from the start.
  > Aesthetics matter when you *communicate*, but they reveal nothing about the data. Understanding the table comes well before styling it.
- Immediately draw a pair plot of every column against every other.
  > A pair plot is a great *overview*, but on an unknown table it can be huge or fail on non-numeric columns; a quick look at the table's shape and types should come first.
- Fit a regression line to the two columns whose names sound related.
  > Fitting a model before you know the data's shape, types, and missing values risks modeling noise or a column that is half empty. Looking comes before fitting.
- [o] Inspect \`.shape\`, \`.head()\`, and \`.isna().sum()\` to learn the size, the columns, and where values are missing.
  > These three checks tell you how big the data is, which columns are numeric versus categorical, and where the holes are, the facts that decide which charts are even possible.

The first move in EDA is always to understand the table itself: its size,
its column types, and its missing values.`}
/>

<MultipleChoice
  markdown={`You want to compare the \`body_mass_g\` distribution across the three penguin
**species** and read each group's median and spread at a glance. Which chart
fits the question best?

- [o] A box (or violin) plot with \`species\` on one axis and \`body_mass_g\` on the other.
  > A box plot lines the groups up side by side and shows each one's median and spread, which is exactly the per-group comparison the question asks for.
- A correlation heatmap of the numeric columns.
  > A heatmap summarizes pairwise correlations between *numeric* columns; it says nothing about how one measurement differs across a categorical group.
- A single histogram of \`body_mass_g\` with no grouping.
  > One pooled histogram blends all three species together, so you cannot read each group's center and spread separately.
- A scatter plot of \`body_mass_g\` against \`flipper_length_mm\`.
  > A scatter relates two *numeric* variables. The question compares one numeric variable across a *category*, which a scatter does not address directly.

When one axis is a category and you want per-group center and spread, the
box/violin family is the direct answer.`}
/>

<MultipleChoice
  markdown={`On a correlation **heatmap**, why is it important to use a *diverging*
palette with \`center=0\` (for example \`cmap="vlag", center=0\`)?

- It hides the diagonal automatically.
  > The palette does not remove the diagonal. The diagonal is always \`1\` and you simply learn to look past it; that is unrelated to centering the colors.
- It forces every correlation to fall between 0 and 1.
  > Correlations already range from \`-1\` to \`+1\` regardless of palette; the colormap only controls how those values are *shown*, not their range.
- It makes the figure load faster.
  > The palette choice is about *meaning*, not performance; rendering speed is unaffected.
- [o] It pins a neutral color to zero so positive and negative correlations read as opposite hues, with strength shown by intensity.
  > Centering a diverging scale at 0 makes \`+0.6\` and \`-0.6\` appear as opposite colors of equal intensity, so the sign and strength of each correlation are read honestly.

A diverging palette centered at zero encodes the *sign* of a correlation in
the hue and its *strength* in the intensity, the honest way to color a
matrix that spans negative to positive.`}
/>

You have now run a complete EDA: you sized up the table, examined single
variables, compared groups, studied relationships, scanned all pairs at
once, and turned your best finding into a figure built to communicate. That
loop, glance, question, chart, notice, repeat, is the whole job, and you
can now run it on any tidy dataset. The final page steps back to recap the
mental models you built and points you toward where to take them next.
