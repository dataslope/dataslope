---
title: Mini Project Walkthrough
description: A complete end-to-end analysis of a real built-in dataset, load, inspect, tidy, transform, summarize, visualize, interpret. This is the workflow every analysis follows.
---

You've now met every piece of the puzzle: vectors, data frames,
dplyr verbs, ggplot, summary statistics, and inference. This
page strings them all together on a real dataset so you can see
the whole arc of an analysis at once.

We'll use the built-in **`airquality`** dataset, daily air
quality measurements from New York during May–September 1973.
Our question:

> **Is ozone higher on hotter days, and how does it vary across
> the summer months?**

That single question will pull us through the entire workflow.

<Figure
  slug="rlang-mini-project-cutout"
  alt="Six stepped platforms rising in sequence from a raw grid to a finished chart, the Dataslope marmot mascot standing at the summit"
/>

## Step 1, Load and inspect

Look before you leap. Always.

<CodeBlock
  adapter="r"
  files={[
    {
      filename: "main.r",
      starterCode: `# airquality is built into R, no loading needed
dim(airquality)
head(airquality)
str(airquality)
summary(airquality)
`,
    },
  ]}
/>

What we learn:

- ~153 rows, 6 columns: `Ozone`, `Solar.R`, `Wind`, `Temp`,
  `Month`, `Day`.
- `Ozone` and `Solar.R` have **NAs**, missing values we'll need
  to handle.
- `Month` is numeric (5 through 9) but conceptually categorical.
- `Temp` is in degrees Fahrenheit.

The rest of the walkthrough follows the standard arc:
**Load → Inspect → Tidy → Transform → Visualize → Interpret.**

## Step 2, Tidy: handle NAs and fix types

<CodeBlock
  adapter="r"
  files={[
    {
      filename: "main.r",
      starterCode: `library(dplyr)

aq <- airquality |>
  filter(!is.na(Ozone)) |> # drop rows missing the response
  mutate(
    Month_name = factor(Month,
      levels = 5:9,
      labels = c("May", "Jun", "Jul", "Aug", "Sep")
    ),
    Temp_C = round((Temp - 32) * 5 / 9, 1)
  )

head(aq)
cat("Rows after dropping Ozone NAs:", nrow(aq), "\\n")
`,
    },
  ]}
/>

A few notes:

- We *dropped* rows missing Ozone because Ozone is the variable
  we want to analyze. (`Solar.R` NAs we'll handle later if
  needed.) Dropping rows is fine **when** you've thought about
  whether NAs are random, for now, assume yes.
- We added a `Month_name` factor with proper ordering, and a
  more reader-friendly `Temp_C` column.

## Step 3, First look: summary statistics

<CodeBlock
  adapter="r"
  files={[
    {
      filename: "main.r",
      starterCode: `library(dplyr)

aq <- airquality |>
  filter(!is.na(Ozone)) |>
  mutate(
    Month_name = factor(Month, levels = 5:9, labels = c("May", "Jun", "Jul", "Aug", "Sep")),
    Temp_C     = round((Temp - 32) * 5 / 9, 1)
  )

# Overall
summary(aq$Ozone)

# By month
monthly <- aq |>
  group_by(Month_name) |>
  summarise(
    n         = n(),
    median    = median(Ozone),
    mean      = round(mean(Ozone), 1),
    sd        = round(sd(Ozone), 1),
    mean_temp = round(mean(Temp_C), 1),
    .groups   = "drop"
  )
print(monthly)
`,
    },
  ]}
/>

Already the pattern emerges: July and August have the highest
mean Ozone *and* the highest mean temperature. May and September
are cooler and cleaner.

## Step 4, Visualize the relationship

Plots will tell us more than summary tables ever can.

<CodeBlock
  adapter="r"
  files={[
    {
      filename: "main.r",
      starterCode: `library(dplyr)
library(ggplot2)

aq <- airquality |>
  filter(!is.na(Ozone)) |>
  mutate(
    Month_name = factor(Month, levels = 5:9, labels = c("May", "Jun", "Jul", "Aug", "Sep")),
    Temp_C     = round((Temp - 32) * 5 / 9, 1)
  )

ggplot(aq, aes(x = Temp_C, y = Ozone, color = Month_name)) +
  geom_point(alpha = 0.7, size = 2) +
  geom_smooth(method = "loess", se = FALSE, color = "black", linewidth = 0.5) +
  labs(
    title    = "Ozone vs. temperature in NYC, May–Sep 1973",
    subtitle = "Each point is one day",
    x        = "Daily max temperature (°C)",
    y        = "Ozone (ppb)",
    color    = "Month"
  ) +
  theme_minimal()
`,
    },
  ]}
/>

The picture is unambiguous: warmer days have higher Ozone. The
relationship looks roughly monotonic, and a bit curvier at high
temperatures.

## Step 5, Look at monthly variation explicitly

<CodeBlock
  adapter="r"
  files={[
    {
      filename: "main.r",
      starterCode: `library(dplyr)
library(ggplot2)

aq <- airquality |>
  filter(!is.na(Ozone)) |>
  mutate(Month_name = factor(Month,
    levels = 5:9,
    labels = c("May", "Jun", "Jul", "Aug", "Sep")
  ))

ggplot(aq, aes(x = Month_name, y = Ozone, fill = Month_name)) +
  geom_boxplot(alpha = 0.85, outlier.alpha = 0.6) +
  labs(
    title = "Ozone distribution by month",
    x = NULL, y = "Ozone (ppb)"
  ) +
  theme_minimal() +
  theme(legend.position = "none")
`,
    },
  ]}
/>

July and August have clearly higher medians and wider spreads,
both more Ozone *and* more variable Ozone. The shoulder months
(May, September) are tighter and lower.

## Step 6, A quick formal check

We *see* a relationship between Temperature and Ozone, but how
strong is it? And is it surprising given the noise?

<CodeBlock
  adapter="r"
  files={[
    {
      filename: "main.r",
      starterCode: `library(dplyr)

aq <- airquality |>
  filter(!is.na(Ozone)) |>
  mutate(Temp_C = (Temp - 32) * 5 / 9)

# Correlation
cor(aq$Temp_C, aq$Ozone)

# Simple linear regression: Ozone ~ Temp
model <- lm(Ozone ~ Temp_C, data = aq)
summary(model)
`,
    },
  ]}
/>

Reading the output:

- The **correlation** is around 0.7, a strong positive linear
  relationship.
- The regression coefficient on `Temp_C` is positive and the
  p-value is *very* small, well below any conventional cutoff.
- The R² (multiple R-squared in the output) tells you what
  fraction of Ozone variation is explained by temperature alone.

We're not making causal claims (Ozone formation has many
drivers!), only describing a strong *association* in this
dataset.

## Step 7, Interpret

Pulling it together in a short, plain-English paragraph (this is
literally the kind of write-up an analyst delivers):

> Across the 1973 New York summer, daily ozone levels rose
> sharply with daily temperature. The relationship is strong
> (correlation ≈ 0.7) and is very unlikely to be a chance pattern
> given the sample size. Ozone was both higher and more variable
> in July and August than in May or September, consistent with
> the known link between heat, sunlight, and photochemical ozone
> production. About half the day-to-day variation in ozone is
> explained by temperature alone; the rest is presumably driven
> by factors not in this dataset, like wind, precursor
> emissions, and atmospheric mixing.

That paragraph is *the deliverable*. Everything before it,
loading, cleaning, plotting, modeling, was scaffolding to make
it trustworthy.

<Figure
  slug="rlang-mini-project-walkthrough-step-7-interpret-inline-cutout"
  alt="One short written slip lifted off the end of a long bench of instruments, and only that slip carried away"
  caption="Everything before the interpretation was scaffolding to make that paragraph trustworthy."
/>

## Putting it all in one script

Here's the entire analysis condensed:

```r
# air-quality-analysis.R
library(dplyr)
library(ggplot2)

# 1. Load + tidy
aq <- airquality |>
  filter(!is.na(Ozone)) |>
  mutate(
    Month_name = factor(Month, levels = 5:9,
                        labels = c("May","Jun","Jul","Aug","Sep")),
    Temp_C     = (Temp - 32) * 5/9
  )

# 2. Summarize
monthly <- aq |>
  group_by(Month_name) |>
  summarise(
    n        = n(),
    median_O = median(Ozone),
    mean_O   = mean(Ozone),
    mean_T   = mean(Temp_C),
    .groups  = "drop"
  )
print(monthly)

# 3. Model
cat("Correlation:", cor(aq$Temp_C, aq$Ozone), "\n")
print(summary(lm(Ozone ~ Temp_C, data = aq)))

# 4. Plot
ggplot(aq, aes(Temp_C, Ozone, color = Month_name)) +
  geom_point(alpha = 0.7) +
  geom_smooth(method = "loess", se = FALSE, color = "black") +
  labs(title = "Ozone vs. temperature, NYC 1973")
```

That's ~25 lines for an entire analysis. *That* is the leverage
R gives you.

## The full challenge

Now you do one yourself. Use the **Palmer penguins** dataset
(already loaded from `penguins.csv`). Compute, for each species, the
**median** and **mean** body mass, into a data frame named
`penguin_summary` with three columns: `species`,
`median_body_mass`, `mean_body_mass`.

<ChallengeCard
  adapter="r"
  title="Per-species body mass summary"
  instructions={`Using dplyr, build \`penguin_summary\` from \`penguins\`: group by \`species\` and summarise to produce columns \`median_body_mass\` and \`mean_body_mass\`. There should be 3 rows (one per species).`}
  entryFilename="main.R"
  datasets={[{ path: "palmer-penguins.csv", stageAs: "penguins.csv" }]}
  files={[
    {
      filename: "main.R",
      initCode: `library(dplyr)\npenguins <- read.csv("penguins.csv")\npenguins <- na.omit(penguins)`,
      starterCode: `penguin_summary <- NULL   # build me

print(penguin_summary)
`,
      solutionCode: `penguin_summary <- penguins |>
  group_by(species) |>
  summarise(
    median_body_mass = median(body_mass_g),
    mean_body_mass   = mean(body_mass_g),
    .groups = "drop"
  )

print(penguin_summary)
`,
    },
  ]}
  tests={[
    {
      id: "exists",
      name: "penguin_summary exists and is a data frame",
      code: `if (!exists("penguin_summary") || !is.data.frame(penguin_summary)) stop("penguin_summary should be a data frame")`,
    },
    {
      id: "rows",
      name: "Three rows (one per species)",
      code: `if (nrow(penguin_summary) != 3) stop(sprintf("expected 3 rows, got %s", nrow(penguin_summary)))`,
    },
    {
      id: "cols",
      name: "Has the required columns",
      code: `if (!all(c("species","median_body_mass","mean_body_mass") %in% names(penguin_summary))) stop("missing required columns")`,
    },
    {
      id: "values",
      name: "Mean values match a direct computation",
      code: `ref <- tapply(penguins$body_mass_g, penguins$species, mean); got <- setNames(penguin_summary$mean_body_mass, as.character(penguin_summary$species)); if (any(abs(ref[names(got)] - got) > 1e-9)) stop("mean_body_mass values are wrong")`,
    },
  ]}
/>

You just did a complete analysis: question → data → cleaning →
summary → visualization → interpretation. That's the loop. Every
real analysis is a longer, deeper version of the same loop.

## Test your understanding

<MultipleChoice
  markdown={`What's the *very first thing* you should do after loading any new dataset?

- Compute the mean of every column.
  > Computing means before inspection misses structural problems like wrong types, missing values, or unexpected scales that would make the means meaningless.
- Make a plot.
  > Plotting before inspection is premature, you won't know which variables matter, which have NAs, or what type they are.
- Run a regression.
  > A regression before inspection can produce results that look plausible but are driven by data-quality problems you never noticed.
- [o] Inspect it, \`dim()\`, \`head()\`, \`str()\`, \`summary()\`, so you understand what's actually there before assuming anything.
  > Skipping inspection is the #1 cause of confidently-wrong analyses.`}
/>

<MultipleChoice
  markdown={`In the analysis above, we showed Ozone ~ Temp is strongly correlated. That means:

- Temperature causes ozone.
  > A correlation tells you two variables move together, not that one drives the other; many factors (sunlight, precursor emissions, mixing) could be responsible.
- The model is broken.
  > A strong correlation (≈ 0.7) with a tiny p-value is exactly what a well-fitting model looks like, not a sign of failure.
- Ozone causes temperature.
  > Ozone is a trace gas produced partly by photochemical reactions, it does not heat the air enough to raise ambient temperature.
- [o] The two vary together strongly in this dataset, a real association, but correlation alone doesn't prove causation; other plausible factors (sunlight, emissions, weather patterns) are not ruled out.
  > Stating associations carefully, and not over-claiming, is core craft.`}
/>

<MultipleChoice
  markdown={`What's the actual *deliverable* of an analysis like this one?

- [o] A clearly-written, honest interpretation of what was found, backed up by reproducible code and clear visualizations.
  > Code and plots are means; communicating an honest finding is the end.
- The R code.
  > The code is the scaffolding that makes results trustworthy and reproducible, but it is not the finding itself.
- A p-value.
  > A p-value is one number from one test, without context, an interpretation, and an understanding of effect size, it tells the reader almost nothing on its own.
- The raw data.
  > Raw data is the input to the analysis, not the output, a stakeholder cannot make a decision from an uninterpreted CSV.`}
/>

You've reached the last hands-on page of the course. What follows
is an optional set of "deeper history" chapters, the full story of
where statistics, statistical computing, and R itself came from,
and then a closing page pointing you toward what to learn next. If
you'd rather skip the history, jump straight to **Next Steps**.
