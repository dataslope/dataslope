---
title: Numbers
description: Integers, floats, complex numbers, arithmetic, and the math module
---

Python ships with three numeric types: arbitrary-precision integers, IEEE 754 floats, and complex numbers. The operators are familiar, but there are a few surprises-especially around floating-point precision.

Every snippet on this page runs in your browser-no setup required.

<svg
  viewBox="0 0 640 200"
  role="img"
  aria-label="A train of digit cells grows straight out past the fixed box meant to bound it, a float hovers in a fuzzy halo just off the ruler's ticks, and one number lives on its own little plane, ints without limits, floats approximate, complex numbers two-dimensional"
  style={{ display: "block", width: "100%", maxWidth: "640px", height: "auto", margin: "2.5rem auto" }}
>
  <g fill="currentColor" opacity="0.1">
    <rect x="90" y="36" width="250" height="60" rx="10" />
  </g>
  <g style={{ color: "var(--ds-blue-500)" }} fill="currentColor">
    <rect x="102" y="49" width="34" height="34" rx="6" fillOpacity="0.85" />
    <rect x="140" y="49" width="34" height="34" rx="6" fillOpacity="0.6" />
    <rect x="178" y="49" width="34" height="34" rx="6" fillOpacity="0.85" />
    <rect x="216" y="49" width="34" height="34" rx="6" fillOpacity="0.6" />
    <rect x="254" y="49" width="34" height="34" rx="6" fillOpacity="0.85" />
    <rect x="292" y="49" width="34" height="34" rx="6" fillOpacity="0.6" />
    <rect x="330" y="49" width="34" height="34" rx="6" fillOpacity="0.85" />
    <rect x="368" y="49" width="34" height="34" rx="6" fillOpacity="0.6" />
    <rect x="406" y="49" width="34" height="34" rx="6" fillOpacity="0.85" />
    <circle cx="456" cy="66" r="4" fillOpacity="0.5" />
    <circle cx="474" cy="66" r="3.5" fillOpacity="0.35" />
    <circle cx="490" cy="66" r="3" fillOpacity="0.25" />
  </g>
  <g fill="currentColor" opacity="0.15">
    <rect x="90" y="150" width="240" height="5" rx="2.5" />
  </g>
  <g fill="currentColor" opacity="0.25">
    <circle cx="110" cy="162" r="2" />
    <circle cx="150" cy="162" r="2" />
    <circle cx="190" cy="162" r="2" />
    <circle cx="230" cy="162" r="2" />
    <circle cx="270" cy="162" r="2" />
    <circle cx="310" cy="162" r="2" />
  </g>
  <g style={{ color: "var(--ds-green-500)" }} fill="currentColor">
    <circle cx="212" cy="140" r="8" />
    <circle cx="212" cy="140" r="15" fillOpacity="0.18" />
    <circle cx="212" cy="140" r="22" fillOpacity="0.08" />
  </g>
  <g fill="currentColor" opacity="0.12">
    <rect x="470" y="110" width="4" height="70" rx="2" />
    <rect x="440" y="158" width="80" height="4" rx="2" />
  </g>
  <g style={{ color: "var(--ds-yellow-500)" }} fill="currentColor">
    <circle cx="502" cy="128" r="8" />
  </g>
  <g fill="currentColor" opacity="0.25">
    <circle cx="502" cy="160" r="2.5" />
    <circle cx="472" cy="128" r="2.5" />
  </g>
</svg>

## Real-world context: Where numbers matter

Numbers are everywhere in code:
- **Web apps:** user IDs, timestamps, request counts, pagination offsets.
- **Data pipelines:** row counts, byte sizes, elapsed time, statistical aggregates.
- **Finance:** prices, quantities, tax rates (and where floats will bite you).
- **Machine learning:** weights, gradients, loss values, learning rates.
- **Games:** positions, velocities, health points, scores.

Understanding the quirks of Python's numeric types-especially the precision limits of floats-will save you from subtle bugs.

## Integers

`int` has **no fixed size**: it grows to fit whatever value you give it. No overflow, no wraparound. This is one of Python's nicest features.

<CodeBlock
  adapter="python"
  files={[
    {
      filename: "main.py",
      starterCode: `big = 2**200
print(big)
print("digits:", len(str(big)))
`,
    },
  ]}
/>

<Callout type="info" title="No integer overflow in Python">
In languages like C or Java, integer types have fixed sizes (e.g., 32 or 64 bits), so `2 ** 100` would overflow. Python's `int` is limited only by available memory. This makes arithmetic safer and more predictable.
</Callout>

Integer literals support several bases:

<CodeBlock
  adapter="python"
  files={[
    {
      filename: "main.py",
      starterCode: `print(0b1010)  # binary -> 10
print(0o17)  # octal  -> 15
print(0xFF)  # hex    -> 255
`,
    },
  ]}
/>

<CodeBlock
  adapter="python"
  files={[
    {
      filename: "main.py",
      starterCode: `# Underscores for readability (Python 3.6+)
print(1_000_000)
print(0b1111_0000)
`,
    },
  ]}
/>

## Floats

`float` is a 64-bit IEEE 754 double-precision floating-point number. That's the same representation used by C, Java, JavaScript, and most other languages. It's fast and space-efficient, but it has precision quirks.

### The 0.1 + 0.2 surprise

<CodeBlock
  adapter="python"
  files={[
    {
      filename: "main.py",
      starterCode: `print(0.1 + 0.2)
print(0.1 + 0.2 == 0.3)
`,
    },
  ]}
/>

<Callout type="warn" title="Why does 0.1 + 0.2 ≠ 0.3?">
In binary, `0.1` is an infinitely repeating fraction (like `1/3` is `0.333...` in decimal). A 64-bit float can only store a finite approximation. When you add two approximations, the error compounds. This is not a Python bug-it's how all IEEE 754 floating-point arithmetic works.

**For safe float comparisons, use `math.isclose`:**

```python
import math
print(math.isclose(0.1 + 0.2, 0.3))  # True
```
</Callout>

<Callout type="info" title="IEEE 754: The standard">
IEEE 754 is the industry-standard specification for floating-point arithmetic, dating back to 1985. It defines how numbers are stored in binary, how operations are rounded, and how special values like `inf` and `NaN` work. Almost every modern CPU has hardware support for it.
</Callout>

### When to avoid floats: finance

For exact decimal arithmetic-like money-use `decimal.Decimal`:

<CodeBlock
  adapter="python"
  files={[
    {
      filename: "main.py",
      starterCode: `from decimal import Decimal

price = Decimal("19.99")
quantity = Decimal("3")
total = price * quantity
print(total)
`,
    },
  ]}
/>

<CodeBlock
  adapter="python"
  files={[
    {
      filename: "main.py",
      starterCode: `from decimal import Decimal

# Float precision fails for currency
print(0.1 + 0.1 + 0.1 == 0.3)

# Decimal is exact
print(Decimal("0.1") + Decimal("0.1") + Decimal("0.1") == Decimal("0.3"))
`,
    },
  ]}
/>

<Callout type="warn" title="Always construct Decimal from strings">
If you pass a float to `Decimal`, you inherit the float's precision error:

```python
Decimal(0.1)  # Decimal('0.1000000000000000055511151231257827021181583404541015625')
```

Always pass a string: `Decimal("0.1")`.
</Callout>

### Fractions for exact rationals

For exact rational arithmetic (fractions), use `fractions.Fraction`:

<CodeBlock
  adapter="python"
  files={[
    {
      filename: "main.py",
      starterCode: `from fractions import Fraction

x = Fraction(1, 3)
y = Fraction(1, 6)
print(x + y)  # 1/2
`,
    },
  ]}
/>

## Complex numbers

Python has built-in support for complex numbers using `j` (not `i`) for the imaginary unit.

<CodeBlock
  adapter="python"
  files={[
    {
      filename: "main.py",
      starterCode: `z = 3 + 4j
print(z.real, z.imag)
print(abs(z))  # magnitude
`,
    },
  ]}
/>

<CodeBlock
  adapter="python"
  files={[
    {
      filename: "main.py",
      starterCode: `z = 3 + 4j
w = 1 + 2j
print(z * w)
print(z / w)
`,
    },
  ]}
/>

## Arithmetic operators

| Operator | Meaning | Example |
| --- | --- | --- |
| `+` | addition | `2 + 3 == 5` |
| `-` | subtraction | `5 - 2 == 3` |
| `*` | multiplication | `2 * 3 == 6` |
| `/` | true division | `7 / 2 == 3.5` |
| `//` | floor division | `7 // 2 == 3` |
| `%` | modulo | `7 % 2 == 1` |
| `**` | exponentiation | `2 ** 10 == 1024` |

A common surprise: `/` **always** returns a float in Python 3, even when both operands are integers.

<CodeBlock
  adapter="python"
  files={[
    {
      filename: "main.py",
      starterCode: `print(7 / 2)  # 3.5 (float)
print(7 // 2)  # 3 (int)
`,
    },
  ]}
/>

Floor division rounds toward negative infinity, which can surprise C programmers:

<CodeBlock
  adapter="python"
  files={[
    {
      filename: "main.py",
      starterCode: `print(7 // 2)  # 3
print(-7 // 2)  # -4, not -3
`,
    },
  ]}
/>

<CodeBlock
  adapter="python"
  files={[
    {
      filename: "main.py",
      starterCode: `print(7 % 2)
print(2**0.5)  # square root via exponentiation
`,
    },
  ]}
/>

Augmented assignment operators work as expected:

<CodeBlock
  adapter="python"
  files={[
    {
      filename: "main.py",
      starterCode: `x = 10
x += 5
x *= 2
print(x)
`,
    },
  ]}
/>

## The `math` module

`math` is in the standard library. It covers the usual suspects:

<CodeBlock
  adapter="python"
  files={[
    {
      filename: "main.py",
      starterCode: `import math

print(math.pi, math.e)
print(math.sqrt(2))
`,
    },
  ]}
/>

<CodeBlock
  adapter="python"
  files={[
    {
      filename: "main.py",
      starterCode: `import math

print(math.sin(math.pi / 2))
print(math.log(100, 10))
print(math.factorial(6))
print(math.gcd(24, 36))
`,
    },
  ]}
/>

<CodeBlock
  adapter="python"
  files={[
    {
      filename: "main.py",
      starterCode: `import math

print(math.floor(3.7), math.ceil(3.2))
print(round(3.5))
`,
    },
  ]}
/>

<Callout type="info" title="Banker's rounding">
Python's `round()` uses "round half to even" (banker's rounding): when a number is exactly halfway between two integers, round to the nearest **even** number. This reduces bias in repeated rounding operations.

```python
print(round(0.5))  # 0
print(round(1.5))  # 2
print(round(2.5))  # 2
```

If you need different rounding behavior, use `math.floor` or `math.ceil`.
</Callout>

## Random numbers

For non-cryptographic randomness, use `random`. For cryptography, use `secrets`.

<CodeBlock
  adapter="python"
  files={[
    {
      filename: "main.py",
      starterCode: `import random

random.seed(42)  # makes output reproducible

print(random.random())  # float in [0.0, 1.0)
print(random.randint(1, 6))  # inclusive on both ends
print(random.choice(["a", "b", "c"]))
`,
    },
  ]}
/>

<CodeBlock
  adapter="python"
  files={[
    {
      filename: "main.py",
      starterCode: `import random

random.seed(42)

print(random.sample(range(100), 5))  # 5 unique items
`,
    },
  ]}
/>

## Challenges

<ChallengeCard
  adapter="python"
  title="Round to cents using Decimal"
  instructions={`Write a function \`round_to_cents(amount)\` that takes a string representing a dollar amount (e.g., \`"19.996"\`) and returns a \`Decimal\` rounded to two decimal places. Use \`Decimal\` and its \`.quantize()\` method with \`ROUND_HALF_UP\`.`}
  files={[
    {
      filename: "main.py",
      starterCode: `from decimal import Decimal, ROUND_HALF_UP


def round_to_cents(amount):
    # Your code here
    return Decimal("0.00")


print(round_to_cents("19.996"))
print(round_to_cents("0.004"))
`,
      solutionCode: `from decimal import Decimal, ROUND_HALF_UP


def round_to_cents(amount):
    d = Decimal(amount)
    return d.quantize(Decimal("0.01"), rounding=ROUND_HALF_UP)


print(round_to_cents("19.996"))
print(round_to_cents("0.004"))
`,
    },
  ]}
  tests={[
    {
      id: "round_up",
      name: "Rounds up correctly",
      code: `from decimal import Decimal; assert round_to_cents("19.996") == Decimal("20.00")`,
    },
    {
      id: "round_down",
      name: "Rounds down correctly",
      code: `from decimal import Decimal; assert round_to_cents("0.004") == Decimal("0.00")`,
    },
    {
      id: "exact",
      name: "Preserves exact values",
      code: `from decimal import Decimal; assert round_to_cents("1.50") == Decimal("1.50")`,
    },
  ]}
/>

<ChallengeCard
  adapter="python"
  title="Greatest common divisor"
  instructions={`Define a function \`gcd(a, b)\` that returns the greatest common divisor of two positive integers using the Euclidean algorithm. Do **not** use \`math.gcd\`, implement it yourself using a loop or recursion.`}
  files={[
    {
      filename: "main.py",
      starterCode: `def gcd(a, b):
    # Your code here
    return 1


print(gcd(48, 18))
print(gcd(100, 25))
`,
      solutionCode: `def gcd(a, b):
    while b:
        a, b = b, a % b
    return a


print(gcd(48, 18))
print(gcd(100, 25))
`,
    },
  ]}
  tests={[
    {
      id: "basic",
      name: "gcd(48, 18) is 6",
      code: `assert gcd(48, 18) == 6`,
    },
    {
      id: "coprime",
      name: "gcd(17, 19) is 1",
      code: `assert gcd(17, 19) == 1`,
    },
    {
      id: "multiple",
      name: "gcd(100, 25) is 25",
      code: `assert gcd(100, 25) == 25`,
    },
  ]}
/>

<ChallengeCard
  adapter="python"
  title="Compound interest"
  instructions={`Write a function \`compound_interest(principal, rate, years)\` that calculates the final amount using the formula \`A = P * (1 + r)^t\`. \`rate\` is a decimal (e.g., \`0.05\` for 5%). Return a float rounded to 2 decimal places.`}
  files={[
    {
      filename: "main.py",
      starterCode: `def compound_interest(principal, rate, years):
    # Your code here
    return 0.0


print(compound_interest(1000, 0.05, 10))
`,
      solutionCode: `def compound_interest(principal, rate, years):
    amount = principal * (1 + rate) ** years
    return round(amount, 2)


print(compound_interest(1000, 0.05, 10))
`,
    },
  ]}
  tests={[
    {
      id: "basic",
      name: "$1000 at 5% for 10 years",
      code: `assert compound_interest(1000, 0.05, 10) == 1628.89`,
    },
    {
      id: "zero_years",
      name: "Zero years returns principal",
      code: `assert compound_interest(1000, 0.05, 0) == 1000.0`,
    },
    {
      id: "high_rate",
      name: "$500 at 10% for 5 years",
      code: `assert compound_interest(500, 0.10, 5) == 805.26`,
    },
  ]}
/>

## Check your understanding

<MultipleChoice
  markdown={`What does \`7 // -2\` evaluate to?

- \`-3\`
  > C-style integer division would truncate toward zero, giving \`-3\`. Python does not do that.
- [o] \`-4\`
  > Floor division rounds toward negative infinity, so \`7 // -2\` is \`-4\`.
- \`3\`
  > That's the absolute value of \`7 // 2\`, but we're dividing by a negative number.
- An error
  > Floor division with a negative divisor is perfectly legal.`}
/>

<MultipleChoice
  markdown={`Why does \`0.1 + 0.2 == 0.3\` evaluate to \`False\`?

- It is a bug in Python that has not been fixed yet.
  > It is not a bug; it is how all IEEE 754 floating-point arithmetic works across all languages.
- [o] Floats are stored in binary and cannot represent these decimals exactly.
  > \`0.1\` and \`0.2\` are infinitely repeating in binary, so their sum is very slightly off from \`0.3\`.
- The \`==\` operator does not work for floats.
  > \`==\` works fine; the issue is that the values are not exactly equal due to precision limits.
- Python rounds floats to two decimal places.
  > Python does not automatically round floats.`}
/>

<MultipleChoice
  markdown={`Which module should you use for exact decimal arithmetic in financial applications?

- \`math\`
  > \`math\` provides functions but still uses \`float\` types.
- \`fractions\`
  > \`fractions\` is for exact rational numbers, but \`decimal\` is more commonly used for currency.
- [o] \`decimal\`
  > \`decimal.Decimal\` provides exact decimal arithmetic suitable for financial calculations.
- \`random\`
  > \`random\` is for generating random numbers, not exact arithmetic.`}
/>

<MultipleChoice
  markdown={`What is the result of \`2 ** 1000\` in Python?

- A very large integer
  > Python does return a very large integer, but the more precise answer specifies there is no overflow.
- [o] A very large integer with no overflow
  > Python's \`int\` has arbitrary precision-no overflow, no wraparound.
- \`OverflowError\`
  > Python's integers never overflow (limited only by memory).
- \`inf\` (infinity)
  > \`inf\` is for floats; integer exponentiation produces an exact integer.`}
/>

Numbers are straightforward once you know the precision gotchas. Strings are deceptively rich; that's next.
