---
title: Filtering Data
description: Boolean masks, the single most important pattern in Pandas, used dozens of times per analysis.
---

If there is one Pandas pattern you will use more than any other,
it is the **boolean mask**. Almost every interesting question
you ask of a dataset starts with "first, give me the rows
where..."

<Figure
  slug="pandas-filtering-data-cutout"
  alt="Filtering data, an isometric illustration"
/>

## The basic shape

A boolean mask is a Series of `True`/`False` values, one per row.
Apply it to a DataFrame and you get back only the rows where the
mask is `True`.

<Figure
  slug="pandas-filtering-data-basic-shape-inline-cutout"
  alt="A board of rows dropping onto a mask, only the rows aligned with open squares passing through"
  caption="The boolean mask is one yes or no per row, and only the yes rows come through."
/>

<CodeBlock
  adapter="python"
  files={[
    {
      filename: "main.py",
      starterCode: `import pandas as pd

df = pd.DataFrame(
    {
        "name": ["Aiko", "Bilal", "Chen", "Diego", "Eve"],
        "dept": ["Eng", "Sales", "Eng", "Ops", "Eng"],
        "salary": [120, 95, 140, 80, 128],
        "tenure": [3, 6, 7, 2, 4],
    }
)

mask = df["salary"] > 100
print("Mask:")
print(mask)
print()

print("Filtered:")
display(df[mask])
`,
    },
  ]}
/>

Two-step or one-step, pick your style:

```python
# Two-step (more readable for complex conditions)
high_earners = df["salary"] > 100
df[high_earners]

# One-step (more compact)
df[df["salary"] > 100]
```

## Comparison operators

All the comparisons return a boolean mask:

<CodeBlock
  adapter="python"
  files={[
    {
      filename: "main.py",
      initCode: `import pandas as pd
df = pd.DataFrame({
    "name":   ["Aiko","Bilal","Chen","Diego","Eve"],
    "dept":   ["Eng","Sales","Eng","Ops","Eng"],
    "salary": [120, 95, 140, 80, 128],
    "tenure": [3, 6, 7, 2, 4],
})`,
      starterCode: `display(df[df["salary"] >= 100])
print()
display(df[df["dept"] == "Eng"])
print()
display(df[df["dept"] != "Eng"])
`,
    },
  ]}
/>

<Figure
  slug="pandas-filtering-data-comparison-operators-inline-cutout"
  alt="A chute with a marked bar across it, blocks taller than the bar stopped and shorter ones passing"
  caption="A comparison sets the bar, and every row is tested against it separately."
/>

## Combining conditions with `&`, `|`, `~`

You must use `&` (and), `|` (or), `~` (not), **not** the Python
keywords `and`, `or`, `not`. And **always** wrap each condition
in parentheses, because `&` has higher precedence than `>`.

<CodeBlock
  adapter="python"
  files={[
    {
      filename: "main.py",
      initCode: `import pandas as pd
df = pd.DataFrame({
    "name":   ["Aiko","Bilal","Chen","Diego","Eve"],
    "dept":   ["Eng","Sales","Eng","Ops","Eng"],
    "salary": [120, 95, 140, 80, 128],
    "tenure": [3, 6, 7, 2, 4],
})`,
      starterCode: `# Engineers AND high earners
display(df[(df["dept"] == "Eng") & (df["salary"] > 120)])
print()

# Engineers OR Sales
display(df[(df["dept"] == "Eng") | (df["dept"] == "Sales")])
print()

# NOT in Engineering
display(df[~(df["dept"] == "Eng")])
`,
    },
  ]}
/>

If you forget the parentheses you will get a cryptic error
about ambiguous truth values. The fix is always the same: add
parentheses.

## Useful helpers

A handful of methods produce boolean masks directly:

### `isin()`, membership

<CodeBlock
  adapter="python"
  files={[
    {
      filename: "main.py",
      initCode: `import pandas as pd
df = pd.DataFrame({
    "name":   ["Aiko","Bilal","Chen","Diego","Eve"],
    "dept":   ["Eng","Sales","Eng","Ops","Eng"],
    "salary": [120, 95, 140, 80, 128],
})`,
      starterCode: `# Cleaner than chaining many "or" clauses
display(df[df["dept"].isin(["Eng", "Sales"])])
`,
    },
  ]}
/>

### `between()`, ranges

<CodeBlock
  adapter="python"
  files={[
    {
      filename: "main.py",
      initCode: `import pandas as pd
df = pd.DataFrame({
    "name":   ["Aiko","Bilal","Chen","Diego","Eve"],
    "dept":   ["Eng","Sales","Eng","Ops","Eng"],
    "salary": [120, 95, 140, 80, 128],
})`,
      starterCode: `display(df[df["salary"].between(90, 130)])
`,
    },
  ]}
/>

### `str.contains()`, substring matching

<CodeBlock
  adapter="python"
  files={[
    {
      filename: "main.py",
      initCode: `import pandas as pd
df = pd.DataFrame({
    "name":   ["Aiko","Bilal","Chen","Diego","Eve"],
    "dept":   ["Eng","Sales","Eng","Ops","Eng"],
})`,
      starterCode: `display(df[df["name"].str.contains("e", case=False)])
`,
    },
  ]}
/>

### `isna()` / `notna()`, missing checks

<CodeBlock
  adapter="python"
  files={[
    {
      filename: "main.py",
      starterCode: `import pandas as pd
import numpy as np

df = pd.DataFrame({"x": [1, 2, np.nan, 4, np.nan]})

print("Missing rows:")
display(df[df["x"].isna()])
print()
print("Non-missing rows:")
display(df[df["x"].notna()])
`,
    },
  ]}
/>

## `query()`, a string-based DSL

Pandas has an alternative filter syntax that some people find
more readable, especially for multi-condition filters:

<CodeBlock
  adapter="python"
  files={[
    {
      filename: "main.py",
      initCode: `import pandas as pd
df = pd.DataFrame({
    "name":   ["Aiko","Bilal","Chen","Diego","Eve"],
    "dept":   ["Eng","Sales","Eng","Ops","Eng"],
    "salary": [120, 95, 140, 80, 128],
})`,
      starterCode: `display(df.query("dept == 'Eng' and salary > 120"))
print()
display(df.query("dept in ['Eng','Sales']"))
`,
    },
  ]}
/>

Whether you prefer `query()` or boolean masks is mostly a style
choice. Most codebases use masks; `query()` shows up in notebooks.

<Figure
  slug="pandas-filtering-data-inline-cutout"
  alt="A board of rows tipping through a grate, the rows that fail the test dropping away and the rest continuing"
  caption="Combining conditions narrows the grate; a row must pass every one of them."
/>

## A real-world filter

<CodeBlock
  adapter="python"
  datasets={[{ path: "palmer-penguins.csv", stageAs: "penguins.csv" }]}
  files={[
    {
      filename: "main.py",
      initCode: `import pandas as pd
penguins = pd.read_csv("penguins.csv")`,
      starterCode: `# Large Gentoo penguins on Biscoe island (over 5 kg)
mask = (
    (penguins["species"] == "Gentoo")
    & (penguins["island"] == "Biscoe")
    & (penguins["body_mass_g"] > 5000)
)

big_gentoos = penguins[mask]
print(f"{len(big_gentoos)} large Gentoos found")
display(big_gentoos[["species", "island", "body_mass_g", "sex"]].head())
`,
    },
  ]}
/>

You will write filters like this all day, every day, as an analyst.
Cosmetic note: breaking each condition onto its own line and
aligning the `&`s makes complex filters readable.

## Filter, then assign

A common pattern: filter to a subset, then add or modify a
column on just that subset. Use `.loc` for the assignment:

<CodeBlock
  adapter="python"
  files={[
    {
      filename: "main.py",
      starterCode: `import pandas as pd

df = pd.DataFrame(
    {
        "name": ["Aiko", "Bilal", "Chen", "Diego", "Eve"],
        "dept": ["Eng", "Sales", "Eng", "Ops", "Eng"],
        "salary": [120, 95, 140, 80, 128],
    }
)

# Flag high-earning engineers
df["flag"] = ""
df.loc[(df["dept"] == "Eng") & (df["salary"] > 120), "flag"] = "high_eng"

display(df)
`,
    },
  ]}
/>

## A filter challenge

<ChallengeCard
  adapter="python"
  title="Filter the penguins dataset"
  instructions={`The penguins file is staged as \`penguins.csv\` and loaded into \`penguins\` for you. Build a DataFrame called \`light_females\` containing **only** penguins that are:

- of species \`Adelie\`, AND
- of \`sex\` equal to \`"female"\`, AND
- lighter than 3500 grams (\`body_mass_g < 3500\`).

Keep all columns.`}
  datasets={[{ path: "palmer-penguins.csv", stageAs: "penguins.csv" }]}
  files={[
    {
      filename: "main.py",
      initCode: `import pandas as pd
penguins = pd.read_csv("penguins.csv")`,
      starterCode: `light_females = None  # TODO

print(len(light_females) if light_females is not None else "still None")
`,
      solutionCode: `light_females = penguins[
    (penguins["species"] == "Adelie")
    & (penguins["sex"] == "female")
    & (penguins["body_mass_g"] < 3500)
]

print(len(light_females))
`,
    },
  ]}
  tests={[
    {
      id: "is_df",
      name: "light_females is a DataFrame",
      description: "Result type check",
      code: `import pandas as pd
assert isinstance(light_females, pd.DataFrame)`,
    },
    {
      id: "species",
      name: "All are Adelie",
      description: "Only Adelie penguins",
      code: `assert (light_females["species"] == "Adelie").all()`,
    },
    {
      id: "sex",
      name: "All are female",
      description: "Only females",
      code: `assert (light_females["sex"] == "female").all()`,
    },
    {
      id: "mass",
      name: "All under 3500 g",
      description: "No penguin 3500 g or heavier",
      code: `assert (light_females["body_mass_g"] < 3500).all()`,
    },
    {
      id: "count",
      name: "Reasonable row count",
      description: "Should be > 0 and < total",
      code: `assert 0 < len(light_females) < 344`,
    },
  ]}
/>

## Check your understanding

<MultipleChoice
  markdown={`Why must you use \`&\` instead of \`and\` when combining two boolean Series?

- It is shorter
  > \`&\` is one character while \`and\` is three; brevity is not the reason, the difference is that \`&\` performs element-wise boolean logic on Series.
- [o] \`and\` operates on single boolean values and tries to coerce a whole Series into one, which is ambiguous, while \`&\` is element-wise on Series
  > \`a and b\` requires \`a\` to be a single True/False, but a Series of length > 1 cannot be reduced to one. Pandas raises the "ambiguous truth value" error.
- \`&\` is faster
  > Speed is not the reason to prefer \`&\`; the reason is correctness, \`and\` raises a ValueError on Series, while \`&\` applies the boolean operation element-wise.
- \`and\` does not exist in Python
  > \`and\` is a standard Python keyword; it exists but operates on single boolean scalars, not on entire Series of booleans.
- Pandas hates English
  > This is a humorous distractor; the restriction exists because Python's \`and\` keyword is semantically defined to work on scalar booleans, not vectorized collections.

Train your fingers: \`&\`, \`|\`, \`~\`, and parentheses around each condition.`}
/>

<MultipleChoice
  markdown={`Which of these is the cleanest way to filter for rows whose \`dept\` column is one of "Eng", "Sales", or "Ops"?

- [o] \`df[df["dept"].isin(["Eng", "Sales", "Ops"])]\`
  > \`isin()\` is the idiomatic Pandas way to express set membership across many values.
- \`df.dept = "Eng" | "Sales" | "Ops"\`
  > This attempts to assign a value to the \`dept\` column rather than filter on it, and \`"Eng" | "Sales"\` is not valid Python for string values.
- \`df[df["dept"] == "Eng" or df["dept"] == "Sales" or df["dept"] == "Ops"]\`
  > Using Python's \`or\` keyword with two Series raises the "ambiguous truth value" error; you must use \`|\` for element-wise boolean OR, but \`isin()\` is still cleaner for multiple values.
- \`df[df["dept"] == ["Eng", "Sales", "Ops"]]\`
  > Comparing a Series to a list with \`==\` does an element-wise equality check between the Series and the list by position, not a membership test, producing a different (usually wrong) result.

\`isin()\` scales cleanly to long lists and is faster than chaining ORs.`}
/>

<MultipleChoice
  markdown={`Why is \`df.loc[mask, "col"] = value\` preferred over \`df[mask]["col"] = value\` when modifying data?

- The second is faster
  > The chained form is not faster; it creates an intermediate copy, which is both slower and potentially wrong.
- [o] The chained form may silently operate on a *copy* and fail to update the original (the SettingWithCopyWarning); \`loc\` guarantees in-place update
  > This is one of the most common silent bugs in Pandas. \`loc\` is the safe way.
- The second is shorter
  > Brevity is not the issue; the chained assignment form is shorter but may silently fail to modify the original DataFrame due to copy-on-write semantics.
- The two are equivalent
  > The chained form \`df[mask]["col"] = value\` may operate on a temporary copy rather than the original DataFrame, meaning the assignment silently has no effect, so they are not equivalent.

When in doubt, use \`loc\` for assignments.`}
/>
