---
title: Loading Datasets
description: Reading real CSV, Excel, JSON, and Parquet data into Pandas, including from URLs, and the most common pitfalls that hit you in the first five seconds.
---

This is where the hands-on work begins. Almost every analysis
starts with the same gesture: read a dataset into a DataFrame.
Pandas makes that easy in the happy path and adds knobs for the
unhappy paths.

<svg
  viewBox="0 0 640 220"
  role="img"
  aria-label="A flat text file of value bars separated by comma dots flows along an arrow into a tidy grid with a blue header row and colored values, read_csv turning text into a DataFrame"
  style={{ display: "block", width: "100%", maxWidth: "640px", height: "auto", margin: "2.5rem auto" }}
>
  <rect x="80" y="40" width="170" height="150" rx="10" fill="currentColor" opacity="0.08" />
  <g fill="currentColor" opacity="0.3">
    <rect x="100" y="62" width="30" height="8" rx="4" />
    <rect x="146" y="62" width="24" height="8" rx="4" />
    <rect x="188" y="62" width="28" height="8" rx="4" />
  </g>
  <g style={{ color: "var(--ds-blue-500)" }} fill="currentColor">
    <rect x="100" y="100" width="28" height="10" rx="5" fillOpacity="0.7" />
    <rect x="100" y="140" width="24" height="10" rx="5" fillOpacity="0.7" />
  </g>
  <g style={{ color: "var(--ds-green-500)" }} fill="currentColor">
    <rect x="146" y="100" width="24" height="10" rx="5" fillOpacity="0.7" />
    <rect x="142" y="140" width="30" height="10" rx="5" fillOpacity="0.7" />
  </g>
  <g style={{ color: "var(--ds-yellow-500)" }} fill="currentColor">
    <rect x="188" y="100" width="30" height="10" rx="5" fillOpacity="0.7" />
    <rect x="190" y="140" width="26" height="10" rx="5" fillOpacity="0.7" />
  </g>
  <g fill="currentColor" opacity="0.45">
    <circle cx="136" cy="108" r="2.5" />
    <circle cx="178" cy="108" r="2.5" />
    <circle cx="132" cy="148" r="2.5" />
    <circle cx="180" cy="148" r="2.5" />
  </g>
  <g style={{ color: "var(--ds-gray-400)" }}>
    <path
      d="M272 115 H 316"
      fill="none"
      stroke="currentColor"
      strokeWidth="2.5"
      strokeLinecap="round"
    />
    <polygon points="316,107 316,123 330,115" fill="currentColor" />
  </g>
  <g style={{ color: "var(--ds-blue-500)" }} fill="currentColor">
    <rect x="370" y="56" width="54" height="26" rx="5" fillOpacity="0.5" />
    <rect x="432" y="56" width="54" height="26" rx="5" fillOpacity="0.5" />
    <rect x="494" y="56" width="54" height="26" rx="5" fillOpacity="0.5" />
  </g>
  <g fill="currentColor" opacity="0.12">
    <rect x="370" y="92" width="54" height="26" rx="5" />
    <rect x="432" y="92" width="54" height="26" rx="5" />
    <rect x="494" y="92" width="54" height="26" rx="5" />
    <rect x="370" y="128" width="54" height="26" rx="5" />
    <rect x="432" y="128" width="54" height="26" rx="5" />
    <rect x="494" y="128" width="54" height="26" rx="5" />
  </g>
  <g style={{ color: "var(--ds-blue-500)" }} fill="currentColor">
    <circle cx="397" cy="105" r="4" />
    <circle cx="397" cy="141" r="4" />
  </g>
  <g style={{ color: "var(--ds-green-500)" }} fill="currentColor">
    <circle cx="459" cy="105" r="4" />
    <circle cx="459" cy="141" r="4" />
  </g>
  <g style={{ color: "var(--ds-yellow-500)" }} fill="currentColor">
    <circle cx="521" cy="105" r="4" />
    <circle cx="521" cy="141" r="4" />
  </g>
</svg>

## The two functions you will use most

- `pd.read_csv(path_or_url)`, the workhorse. CSV is the
  universal data format.
- `pd.read_excel(path_or_url)`, for `.xlsx` files.

Honorable mentions:

- `pd.read_json`, JSON, often after some flattening.
- `pd.read_parquet`, Parquet files (a fast columnar format).
- `pd.read_sql`, query results from a SQL database.

For the rest of the course we will lean on `read_csv` because the
data we will use lives on the public internet as CSVs.

## Meet the penguins

Our main companion dataset for the course is **Palmer Penguins**:
344 penguins observed near Palmer Station in Antarctica. It was
collected by Dr. Kristen Gorman at the Palmer Station Long-Term
Ecological Research site, and packaged by Allison Horst and
colleagues as a friendly, ethically-uncomplicated alternative to
the famous old iris flower dataset. It is small, it is real, and,
crucially for a course that takes data cleaning seriously, it has a
few genuinely missing values. We will get to know it well.

## Reading from a URL

`pd.read_csv` accepts a URL exactly like a file path. The bytes are
fetched over HTTPS in the background and parsed. This is the one
place in the course where we will read straight from a URL, so you
can see how it works:

<CodeBlock
  adapter="python"
  files={[
    {
      filename: "main.py",
      starterCode: `import pandas as pd

# Direct URL, works because raw.githubusercontent.com supports CORS
penguins = pd.read_csv(
    "https://raw.githubusercontent.com/dataslope/datasets/main/palmer-penguins.csv"
)

print("Shape:", penguins.shape)
print("First 3 rows:")
penguins.head(3)
`,
    },
  ]}
/>

Two notes:

1. **Use the raw URL.** Browsers show GitHub *HTML pages* at
   `github.com/.../file.csv`, that is not the file itself. The raw
   version starts with `raw.githubusercontent.com` and returns the
   actual bytes.
2. **Some hosts don't allow direct browser fetches.** For those you
   can route through a CORS proxy.

For the rest of the course, instead of hardcoding URLs we will
*stage* the file as a local `penguins.csv` (you will see it loaded
with `pd.read_csv("penguins.csv")`). That keeps the lesson code
clean and is closer to how you will work on your own machine,
where the data is a file on disk, not a URL.

## Common `read_csv` options

`pd.read_csv` has dozens of parameters, but you will use only a
handful in real life:

| Parameter        | What it does                                          |
| ---------------- | ----------------------------------------------------- |
| `sep`            | Custom delimiter (default `,`). Use `"\t"` for TSV.   |
| `header`         | Which row contains the header (default `0`).          |
| `names`          | Use these column names (skip header).                 |
| `usecols`        | Only load these columns.                              |
| `dtype`          | Force types (`{"zip": "string"}`).                    |
| `parse_dates`    | Parse these columns as dates.                         |
| `na_values`      | Treat these strings as missing (`["NA", "-"]`).       |
| `skiprows`       | Skip N rows at the top (or specific rows).            |
| `nrows`          | Only read N rows (for sampling huge files).           |
| `encoding`       | Text encoding (`"utf-8"`, `"latin-1"`, ...).          |

Let us try a real example with several of these, staging the
penguins file as a local `penguins.csv`.

<CodeBlock
  adapter="python"
  datasets={[{ path: "palmer-penguins.csv", stageAs: "penguins.csv" }]}
  files={[
    {
      filename: "main.py",
      starterCode: `import pandas as pd

penguins = pd.read_csv(
    "penguins.csv",
    usecols=["species", "island", "body_mass_g", "flipper_length_mm", "sex"],
    dtype={"species": "category", "island": "category", "sex": "category"},
)

print(penguins.dtypes)
print()
print(penguins.head())
`,
    },
  ]}
/>

Two things to notice:

- `usecols` saves memory by skipping columns we do not need.
- `dtype` with `"category"` for repeated string columns (`species`,
  `island`, `sex`) saves memory and speeds up later groupings,
  there are only a handful of distinct values across hundreds of
  rows.

## The first three things to do after `read_csv`

Whenever you load a new dataset, run these three commands
*before* you do anything else.

<CodeBlock
  adapter="python"
  datasets={[{ path: "palmer-penguins.csv", stageAs: "penguins.csv" }]}
  files={[
    {
      filename: "main.py",
      initCode: `import pandas as pd
penguins = pd.read_csv("penguins.csv")`,
      starterCode: `# 1. Shape, do you have the rows you expected?
print("Shape:", penguins.shape)
print()

# 2. Head, does each column look sensible?
print(penguins.head())
print()

# 3. Dtypes, are the types right?
print(penguins.dtypes)
`,
    },
  ]}
/>

If any of those three looks wrong, surprising row count,
garbled values, wrong types, fix it now. The cost of fixing a
loading bug is much lower at this moment than three steps later.

## When a URL won't load directly

Reading from a URL only works when the host returns the right
permission headers (the technical name is **CORS**, cross-origin
resource sharing). `raw.githubusercontent.com` does;
`github.com/.../file.csv` (the HTML page) does not, and neither do
many private servers. When a browser-side fetch is blocked, the
usual fix is to route the request through a small **CORS proxy**, a
relay that adds the missing headers, by URL-encoding the real
address into a `?url=...` parameter:

```python
import pandas as pd

# The original URL is URL-encoded into the ?url=... parameter
proxy = "https://your-cors-proxy.example.com/?url="
real = "https%3A%2F%2Fexample.com%2Fdata.csv"
df = pd.read_csv(proxy + real)
```

When you have a choice, **prefer the direct
`raw.githubusercontent.com` URL** (or, as in this course, a staged
local file). A proxy is a workaround for the cases where you do
not. On your own laptop the problem usually disappears entirely,
local Python is not a browser and is not bound by CORS at all.

## Reading Excel files

`.xlsx` files often have multiple sheets and mixed content
(headers offset by a few rows, merged cells, etc.). The basic
call:

```python
df = pd.read_excel("file.xlsx", sheet_name="Sales")
```

`sheet_name` accepts:

- A string (sheet name).
- An integer (sheet index, 0 = first).
- A list of sheet names → returns a dict of DataFrames.
- `None` → returns *all* sheets as a dict.

A note on reality: Excel files frequently have a row or two of
title text at the top before the actual header row. Use the
`skiprows` parameter to jump over them.

## Reading JSON

JSON often comes from APIs and is *hierarchical*, not tabular.
Pandas can flatten simple cases:

<CodeBlock
  adapter="python"
  files={[
    {
      filename: "main.py",
      starterCode: `import pandas as pd
import io

json_text = """
[
  {"name": "Aiko",  "age": 29, "city": "Tokyo"},
  {"name": "Bilal", "age": 42, "city": "Karachi"},
  {"name": "Chen",  "age": 35, "city": "Shanghai"}
]
"""

df = pd.read_json(io.StringIO(json_text))
print(df)
print(df.dtypes)
`,
    },
  ]}
/>

For deeply nested JSON, use `pd.json_normalize(...)`, it
recursively flattens nested objects into columns with
dot-separated names.

## Defensive reading patterns

A few habits worth building from day one.

### Always pin the types you care about

```python
df = pd.read_csv(
    path,
    dtype={"zip": "string", "phone": "string"},
)
```

This protects you from the leading-zero / scientific-notation
problems we saw earlier.

### Parse dates explicitly

```python
df = pd.read_csv(path, parse_dates=["order_date", "ship_date"])
```

Strings that look like dates are still strings until you parse
them. Date arithmetic on strings does nothing useful.

### Mark known missing-value sentinels

Many CSVs use `"N/A"`, `"-"`, `"unknown"` to mean missing.
Tell Pandas:

```python
df = pd.read_csv(path, na_values=["N/A", "-", "unknown"])
```

### Sample huge files first

If a file is 5 GB, do not load it all to see what is in it.
Load the first 1000 rows:

```python
preview = pd.read_csv(path, nrows=1000)
```

Inspect, design your real load, then run it.

## A loading challenge

<ChallengeCard
  adapter="python"
  title="Load and trim the penguins dataset"
  instructions={`The penguins file is staged for you as \`penguins.csv\`. Load it, but:

- Only load the columns \`species\`, \`island\`, \`body_mass_g\`, \`flipper_length_mm\`, and \`sex\`.
- Convert \`species\` and \`island\` to the \`category\` dtype as you load.

Store the result in a DataFrame called \`df\`.`}
  datasets={[{ path: "palmer-penguins.csv", stageAs: "penguins.csv" }]}
  files={[
    {
      filename: "main.py",
      starterCode: `import pandas as pd

df = None  # TODO: read penguins.csv with usecols and dtype

print(df.head() if df is not None else "df is still None")
`,
      solutionCode: `import pandas as pd

df = pd.read_csv(
    "penguins.csv",
    usecols=["species", "island", "body_mass_g", "flipper_length_mm", "sex"],
    dtype={"species": "category", "island": "category"},
)

print(df.head())
`,
    },
  ]}
  tests={[
    {
      id: "is_dataframe",
      name: "df is a DataFrame",
      description: "The variable df is a pandas DataFrame",
      code: `import pandas as pd
assert isinstance(df, pd.DataFrame), "df should be a DataFrame"`,
    },
    {
      id: "correct_columns",
      name: "Has exactly the requested columns",
      description: "species, island, body_mass_g, flipper_length_mm, sex",
      code: `expected = {"species", "island", "body_mass_g", "flipper_length_mm", "sex"}
actual = set(df.columns)
assert actual == expected, f"Got {sorted(actual)}, expected {sorted(expected)}"`,
    },
    {
      id: "row_count",
      name: "Loaded all 344 rows",
      description: "The penguins dataset has 344 rows",
      code: `assert len(df) == 344, f"Expected 344 rows, got {len(df)}"`,
    },
    {
      id: "category_dtypes",
      name: "species and island are categorical",
      description: "Both columns should have dtype 'category'",
      code: `import pandas as pd
assert isinstance(df["species"].dtype, pd.CategoricalDtype)
assert isinstance(df["island"].dtype, pd.CategoricalDtype)`,
    },
  ]}
/>

## Check your understanding

<MultipleChoice
  markdown={`What is the difference between \`https://github.com/.../file.csv\` and \`https://raw.githubusercontent.com/.../file.csv\`?

- They are identical
  > The two URLs serve different content: the first is a GitHub webpage with HTML, JavaScript, and navigation; the second is the raw file bytes.
- The first is faster
  > The first URL typically returns more data (a full HTML page) than the second (the raw file), so it is not faster, and passing HTML to \`pd.read_csv\` causes a parse error.
- [o] The first returns the GitHub HTML page that *displays* the file; the second returns the file's raw bytes
  > Always use the raw URL for \`pd.read_csv\` so you get the data and not the HTML.
- The second requires authentication
  > Public \`raw.githubusercontent.com\` URLs are freely accessible without a token; authentication is only needed for private repositories.

You will hit this distinction the very first time you try to load data from a GitHub link.`}
/>

<MultipleChoice
  markdown={`Why use the \`category\` dtype on a column like \`species\`?

- It looks prettier
  > The \`category\` dtype does not change how values are displayed; the visual output of a \`print\` or \`head\` call looks the same as a regular string column.
- [o] Repeated string values are stored once with integer codes, which saves memory and speeds up grouping
  > \`category\` is essentially an interned-string column, ideal for low-cardinality repeated values.
- It allows the column to hold any type
  > The \`category\` dtype is more restrictive, not more permissive, it represents a fixed set of values (the categories), not arbitrary types.
- It is required by Pandas
  > \`category\` is an optional optimization; Pandas defaults to \`object\` dtype for string columns and works fine without it.

For columns with millions of rows and only a handful of distinct values, the savings can be 10x or more.`}
/>

<MultipleChoice
  markdown={`A CSV column \`phone\` contains values like \`5551234567\`. After \`pd.read_csv\` you see it has dtype \`int64\`. What is the safest fix?

- Restart your computer
  > Restarting the computer does not change how \`pd.read_csv\` infers dtypes; the same file will still be parsed as \`int64\` on the next run.
- Convert to float
  > Converting to \`float64\` does not preserve phone number strings; it makes the values harder to work with and can introduce scientific notation for long numbers.
- [o] Pass \`dtype={"phone": "string"}\` to \`pd.read_csv\` so the values are read as strings from the start
  > Once a leading-zero phone or ZIP code has been parsed as int, the leading zeros are gone. Force the string type at read time.
- Use \`.astype("int64")\` afterwards
  > Converting to \`int64\` after the fact moves in the wrong direction, phone numbers should be strings, and the column is already an integer at this point.

Specifying \`dtype\` at read time is much safer than trying to recover later.`}
/>

<MultipleChoice
  markdown={`When loading a 5 GB CSV that you have never seen before, what is the recommended first move?

- Load the whole thing and wait
  > Loading 5 GB into memory without first understanding the file's structure risks out-of-memory errors and wastes time before you even know what columns you need.
- Open it in Excel
  > Excel cannot open a 5 GB CSV, it would hit its row limit or run out of memory long before loading the full file.
- [o] Use \`pd.read_csv(path, nrows=1000)\` to load a preview, then plan the real load (dtypes, usecols) based on what you see
  > A small preview is the cheapest way to design a defensible full load.
- Convert it to JSON first
  > Converting a 5 GB CSV to JSON produces a larger file in a slower-to-parse format; it adds work without improving your understanding of the data.

Cheap previews save expensive loads.`}
/>
