---
title: First Look at a Dataset
description: The five-minute ritual every analyst performs on a new dataset, and the questions to ask before you compute a single number.
---

You have a fresh DataFrame. The temptation is to dive straight
into `.mean()` and charts. **Resist it.** Spend five minutes
getting to know the dataset first. Those five minutes save hours
later.

<svg
  viewBox="0 0 640 230"
  role="img"
  aria-label="A data grid rendered in fading blue shades beside a single yellow index bar: the header and first rows are bright with blue value dots while the rows below dissolve step by step, the first look at the head of a dataset"
  style={{ display: "block", width: "100%", maxWidth: "640px", height: "auto", margin: "2.5rem auto" }}
>
  <g style={{ color: "var(--ds-yellow-500)" }} fill="currentColor">
    <rect x="118" y="30" width="6" height="88" rx="3" fillOpacity="0.8" />
  </g>
  <g style={{ color: "var(--ds-blue-500)" }} fill="currentColor">
    <rect x="152" y="30" width="72" height="24" rx="5" fillOpacity="0.55" />
    <rect x="236" y="30" width="72" height="24" rx="5" fillOpacity="0.55" />
    <rect x="320" y="30" width="72" height="24" rx="5" fillOpacity="0.55" />
    <rect x="404" y="30" width="72" height="24" rx="5" fillOpacity="0.55" />
  </g>
  <g style={{ color: "var(--ds-blue-500)" }} fill="currentColor" fillOpacity="0.28">
    <rect x="152" y="62" width="72" height="24" rx="5" />
    <rect x="236" y="62" width="72" height="24" rx="5" />
    <rect x="320" y="62" width="72" height="24" rx="5" />
    <rect x="404" y="62" width="72" height="24" rx="5" />
  </g>
  <g style={{ color: "var(--ds-blue-500)" }} fill="currentColor" fillOpacity="0.2">
    <rect x="152" y="94" width="72" height="24" rx="5" />
    <rect x="236" y="94" width="72" height="24" rx="5" />
    <rect x="320" y="94" width="72" height="24" rx="5" />
    <rect x="404" y="94" width="72" height="24" rx="5" />
  </g>
  <g style={{ color: "var(--ds-blue-500)" }} fill="currentColor" fillOpacity="0.5">
    <circle cx="188" cy="74" r="4" />
    <circle cx="356" cy="74" r="4" />
    <circle cx="272" cy="106" r="4" />
    <circle cx="440" cy="106" r="4" />
  </g>
  <g style={{ color: "var(--ds-blue-500)" }} fill="currentColor">
    <circle cx="272" cy="74" r="4" />
    <circle cx="440" cy="74" r="4" />
    <circle cx="188" cy="106" r="4" />
    <circle cx="356" cy="106" r="4" />
  </g>
  <g style={{ color: "var(--ds-blue-500)" }} fill="currentColor" fillOpacity="0.12">
    <rect x="152" y="126" width="72" height="24" rx="5" />
    <rect x="236" y="126" width="72" height="24" rx="5" />
    <rect x="320" y="126" width="72" height="24" rx="5" />
    <rect x="404" y="126" width="72" height="24" rx="5" />
  </g>
  <g style={{ color: "var(--ds-blue-500)" }} fill="currentColor" fillOpacity="0.07">
    <rect x="152" y="158" width="72" height="24" rx="5" />
    <rect x="236" y="158" width="72" height="24" rx="5" />
    <rect x="320" y="158" width="72" height="24" rx="5" />
    <rect x="404" y="158" width="72" height="24" rx="5" />
  </g>
  <g style={{ color: "var(--ds-blue-500)" }} fill="currentColor" fillOpacity="0.04">
    <rect x="152" y="190" width="72" height="24" rx="5" />
    <rect x="236" y="190" width="72" height="24" rx="5" />
    <rect x="320" y="190" width="72" height="24" rx="5" />
    <rect x="404" y="190" width="72" height="24" rx="5" />
  </g>
</svg>

## The five-minute ritual

Every analyst eventually develops a personal version of this
ritual. Here is a solid one to start with.

<Steps>
<Step>

**Shape and head**

How big is the table, and what do the first rows look like?

</Step>
<Step>

**Dtypes and memory**

Did Pandas read each column as the right type, and will it fit in memory?

</Step>
<Step>

**Describe (numeric + objects)**

Summary statistics for the numeric columns, then the text/categorical ones.

</Step>
<Step>

**Missing-value map**

Where is data missing, and is the pattern random or structural?

</Step>
<Step>

**Unique-value counts for categoricals**

For every text-like column, what distinct values actually appear?

</Step>
</Steps>

Let us walk each step on a real dataset.

## Step 1, Shape and head

The first question: *what does the table look like, and how big
is it?*

<CodeBlock
  adapter="python"
  datasets={[{ path: "palmer-penguins.csv", stageAs: "penguins.csv" }]}
  files={[
    {
      filename: "main.py",
      initCode: `import pandas as pd
penguins = pd.read_csv("penguins.csv")`,
      starterCode: `print("Shape (rows, columns):", penguins.shape)
print()
print("Column names:")
for col in penguins.columns:
    print(" -", col)
print()
print("First 5 rows:")
penguins.head()
`,
    },
  ]}
/>

What you are looking for:

- **Reasonable row count.** Does 344 penguins match what someone
  told you to expect? If you expected ten thousand and got a few
  hundred, something was filtered.
- **All the columns you expected.** If you were promised a
  `nest_id` and there is no such column, ask.
- **Sample values that make sense.** If `body_mass_g` shows `None`,
  `"N/A"`, or a wildly out-of-range value, you know cleaning is in
  your future. (Look closely at row index 3 in the output, it is
  almost all `NaN`.)

## Step 2, Dtypes and memory

The second question: *did Pandas interpret the columns correctly,
and is the dataset going to fit in memory?*

<CodeBlock
  adapter="python"
  datasets={[{ path: "palmer-penguins.csv", stageAs: "penguins.csv" }]}
  files={[
    {
      filename: "main.py",
      initCode: `import pandas as pd
penguins = pd.read_csv("penguins.csv")`,
      starterCode: `print(penguins.dtypes)
print()
penguins.info(memory_usage="deep")
`,
    },
  ]}
/>

`info(memory_usage="deep")` prints a summary including estimated
memory in bytes (the `"deep"` flag inspects each string instead of
guessing). Notice that `body_mass_g` is `float64` even though body
mass is conceptually a whole number of grams, that is Pandas
telling you the column contains missing values, since `NaN` only
lives in float columns.

Look for:

- **Strings that should be numbers** (e.g. `"5,000"` with a comma
  comes in as `object`).
- **Numbers that should be strings** (ZIP codes, phone numbers).
- **Object columns that should be categorical** (a small, repeating
  vocabulary like `species` or `island`).
- **`object` columns that contain dates**, they need
  `pd.to_datetime`.

## Step 3, Describe

`describe` gives you summary statistics for each column. By
default it summarizes numeric columns; pass `include="all"` to
also describe text/categorical columns.

<CodeBlock
  adapter="python"
  datasets={[{ path: "palmer-penguins.csv", stageAs: "penguins.csv" }]}
  files={[
    {
      filename: "main.py",
      initCode: `import pandas as pd
penguins = pd.read_csv("penguins.csv")`,
      starterCode: `print("Numeric columns:")
print(penguins.describe().round(1))
print()
print("Categorical / object columns:")
print(penguins.describe(include=["object"]))
`,
    },
  ]}
/>

For numeric columns, look at:

- **`count`**, should match `len(df)` unless there are NaNs.
- **`min` / `max`**, sanity check the range. Age of -3?
  Salary of $999,999,999? Bug.
- **`mean` vs `50%` (median)**, large differences signal skew
  or outliers.
- **`std`**, very small std means the column barely varies and
  may not be useful.

For object columns:

- **`unique`**, number of distinct values. Useful for spotting
  near-categoricals.
- **`top`**, most common value.
- **`freq`**, how many times the top value appears.

## Step 4, Missing-value map

The fourth question: *where is data missing?*

<CodeBlock
  adapter="python"
  datasets={[{ path: "palmer-penguins.csv", stageAs: "penguins.csv" }]}
  files={[
    {
      filename: "main.py",
      initCode: `import pandas as pd
penguins = pd.read_csv("penguins.csv")`,
      starterCode: `# Count missing values per column
missing = penguins.isna().sum()
missing_pct = (penguins.isna().mean() * 100).round(1)

summary = pd.DataFrame({"missing": missing, "pct": missing_pct})
print(summary[summary["missing"] > 0].sort_values("missing", ascending=False))
print()
print("Columns with no missing values:", (missing == 0).sum())
`,
    },
  ]}
/>

The penguins dataset has a small but instructive amount of
missingness: `sex` is missing for 11 penguins, and the four body
measurements are each missing for 2. That is not random noise, two
penguins are missing *every* measurement (the observer never got a
full reading), while the other 9 are missing only `sex` (which can
be harder to determine in the field). The pattern matters:

- **Random missingness** can often be ignored or imputed.
- **Structural missingness** (a column that is only populated
  for some rows by design) tells you something about the
  dataset's grain.
- **Concentrated missingness** (one row is mostly NaN) may
  indicate a bad row that should be dropped.

We have a whole chapter on this.

## Step 5, Unique-value counts for categoricals

For every text-like column, look at the values:

<CodeBlock
  adapter="python"
  datasets={[{ path: "palmer-penguins.csv", stageAs: "penguins.csv" }]}
  files={[
    {
      filename: "main.py",
      initCode: `import pandas as pd
penguins = pd.read_csv("penguins.csv")`,
      starterCode: `for col in penguins.select_dtypes(include="object").columns:
    counts = penguins[col].value_counts(dropna=False)
    n_unique = penguins[col].nunique()
    print(f"--- {col} ({n_unique} unique) ---")
    print(counts.head(8))
    print()
`,
    },
  ]}
/>

What to look for:

- **Casing inconsistencies**, `"Sales"`, `"sales"`,
  `"SALES"`.
- **Typos**, `"Engineering"`, `"Enginering"`.
- **Near-duplicates**, `"USA"`, `"U.S."`, `"United States"`.
- **Surprising rare values**, a single row with department
  `"???"` is probably a data-entry mistake.
- **Cardinality**, if every row has a unique value (like a
  name), this is an *identifier* column, not a category.

## A second-look ritual: pairwise interactions

After the five-minute ritual, do a few quick cross-tabulations.

<CodeBlock
  adapter="python"
  datasets={[{ path: "palmer-penguins.csv", stageAs: "penguins.csv" }]}
  files={[
    {
      filename: "main.py",
      initCode: `import pandas as pd
penguins = pd.read_csv("penguins.csv")`,
      starterCode: `# Which species live on which island?
print("Species x island counts:")
print(pd.crosstab(penguins["species"], penguins["island"]))
print()

# How many distinct islands does each species appear on?
print("Islands per species:")
print(penguins.groupby("species")["island"].nunique())
`,
    },
  ]}
/>

These take a minute and often reveal the *structure* of the
dataset. Here the crosstab tells a real ecological story: Gentoos
appear only on Biscoe, Chinstraps only on Dream, while Adelies live
on all three islands. That means **species and island are not
independent**, a fact you must remember before claiming, say, that
"Biscoe penguins are heavier" (they are mostly Gentoos, which are
heavier *everywhere*). That kind of insight changes how you think
about every later analysis.

## Document what you find

A common professional habit: keep a short Markdown note (in the
notebook itself or in a separate file) capturing your findings
from the five-minute ritual.

> *Dataset: palmer-penguins.csv. 344 rows × 8 cols. Each row is one
> penguin. `sex` missing for 11; all four measurements missing for
> 2 penguins. `species` has 3 values (Adelie 152, Gentoo 124,
> Chinstrap 68). Species and island are linked, Gentoo only on
> Biscoe, Chinstrap only on Dream, Adelie on all three. Years
> 2007–2009.*

The next person who picks up your notebook will thank you. The
next person is often *you* in three months.

<Callout type="info" title="The single most under-rated skill">
The analysts who get promoted are usually the ones who can
quickly summarize "what is in this dataset?" in plain English.
The technical skills come naturally with practice; the habit of
*looking before computing* is much rarer.
</Callout>

## A guided exploration challenge

<ChallengeCard
  adapter="python"
  title="First-look summary"
  instructions={`The penguins file is staged as \`penguins.csv\` and already loaded into \`penguins\` for you. Compute four values and assign them to these variables:

1. \`n_rows\`, total number of penguins
2. \`n_cols\`, total number of columns
3. \`n_missing_total\`, total count of NaN cells across the whole DataFrame (sum over all columns), as a Python \`int\`
4. \`species_list\`, a sorted list of the unique values in the \`species\` column

Use Pandas, not hard-coded numbers.`}
  datasets={[{ path: "palmer-penguins.csv", stageAs: "penguins.csv" }]}
  files={[
    {
      filename: "main.py",
      initCode: `import pandas as pd
penguins = pd.read_csv("penguins.csv")`,
      starterCode: `n_rows = None
n_cols = None
n_missing_total = None
species_list = None

print(n_rows, n_cols, n_missing_total, species_list)
`,
      solutionCode: `n_rows = penguins.shape[0]
n_cols = penguins.shape[1]
n_missing_total = int(penguins.isna().sum().sum())
species_list = sorted(penguins["species"].unique().tolist())

print(n_rows, n_cols, n_missing_total, species_list)
`,
    },
  ]}
  tests={[
    {
      id: "n_rows",
      name: "n_rows == 344",
      description: "The penguins dataset has 344 rows",
      code: `assert n_rows == 344, f"Got n_rows = {n_rows}"`,
    },
    {
      id: "n_cols",
      name: "n_cols == 8",
      description: "The dataset has 8 columns",
      code: `assert n_cols == 8, f"Got n_cols = {n_cols}"`,
    },
    {
      id: "missing",
      name: "n_missing_total counts every NaN cell",
      description: "Use penguins.isna().sum().sum()",
      code: `assert isinstance(n_missing_total, int), "n_missing_total should be a Python int"
assert n_missing_total == 19, f"Expected 19 missing cells, got {n_missing_total}"`,
    },
    {
      id: "species_list",
      name: "species_list is a sorted list of unique species",
      description: "Sorted list of unique species values",
      code: `assert isinstance(species_list, list), "species_list must be a list"
assert species_list == sorted(set(species_list)), "species_list must be sorted and unique"
assert species_list == ["Adelie", "Chinstrap", "Gentoo"], f"Got {species_list}"`,
    },
  ]}
/>

## Check your understanding

<MultipleChoice
  markdown={`Why does the chapter recommend running \`df.describe()\` before any "real" analysis?

- To make the dataset look bigger
  > \`df.describe()\` summarizes statistics; it does not change the number of rows in the dataset.
- [o] Because it gives a quick read on ranges, central tendency, and spread, and surfaces obviously wrong values (negative ages, impossible maxes) early
  > A 30-second \`describe\` regularly catches data bugs that would otherwise hide for hours.
- It is required by Pandas
  > \`df.describe()\` is a recommended habit, not a mandatory Pandas step; code runs without it.
- It removes missing values
  > \`df.describe()\` skips NaN values when computing statistics but does not drop them from the DataFrame.

Cheap eyeball checks at the front of an analysis prevent expensive corrections later.`}
/>

<MultipleChoice
  markdown={`In the missing-value step, why is the **pattern** of missingness as important as the count?

- It is not
  > The chapter explicitly argues the pattern is important; structural or concentrated missingness calls for different remedies than random missingness.
- [o] Because random missingness can often be ignored or imputed, structural missingness reveals something about the dataset's grain, and concentrated missingness flags bad rows, each pattern suggests a different fix
  > Treating all NaNs the same way is a common rookie mistake. Look at *where* they cluster before deciding how to handle them.
- It changes how Pandas computes means
  > Pandas skips NaN values in aggregations regardless of whether they are random or structural; the pattern affects *your* analytical decisions, not Pandas's arithmetic.
- Pandas requires you to look at the pattern
  > Pandas does not enforce any particular NaN-pattern inspection; the recommendation comes from analytical best practice, not from the library.

Same headline count of NaNs can mean very different things depending on distribution.`}
/>

<MultipleChoice
  markdown={`A 344-row table has a column whose \`.value_counts()\` shows 344 distinct values, one per row. What is it most likely to be?

- A categorical variable
  > A categorical column has a small, repeating set of values, not a unique value for every row.
- A boolean column
  > A boolean column has at most two distinct values (\`True\`/\`False\`), not 344 unique ones.
- [o] An identifier column (like a tag number or record ID), every row has a different value, so it is unique per row
  > Such columns are useful as keys (for joins) but not for grouping or counting.
- A date column
  > A date column can have many unique values, but one distinct value per row more strongly signals an identifier column than a date series, which usually repeats.

Recognizing "this is an ID, not a category" early stops you from making meaningless \`groupby(id_column)\` calls.`}
/>

<MultipleChoice
  markdown={`Which of these is the chapter's recommended habit *after* completing the five-minute ritual?

- Delete the dataset
  > Deleting the dataset would destroy the raw input that all subsequent analysis depends on; the chapter advises keeping the raw data sacred.
- Switch to Excel
  > The chapter's recommended post-ritual habit is documentation, not a tool switch.
- [o] Write a short Markdown note summarizing what you learned (rows, grain, missingness, surprises) so you and others can find it later
  > A few sentences of plain-English summary are the highest-leverage documentation you can produce.
- Run all the visualizations
  > Jumping straight to visualizations skips the documentation step the chapter recommends; a prose summary is more reusable and searchable than a batch of charts.

Compact prose summaries pay massive dividends in real projects.`}
/>
